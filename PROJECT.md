# CurseForge ModJam 2026 模组项目

> 当前英文名称：`Echo Warrior`
> 名称未来可以调整；首阶段固定技术标识为 `echo_warrior`，Java 根包为 `com.yuriscat.echowarrior`。

## 1. 项目目标

为 CurseForge Minecraft ModJam 2026 制作一个围绕“过去的回声”展开的 Java 模组。

核心体验：玩家通过考古发现英灵遗物，将遗物装入召唤器，短暂召回过去时代的战士幻影，让它们跟随并协助战斗。玩家随后可以培养英灵、改造召唤器并继续寻找更好的遗物。

核心循环：

```text
探索 → 回声定位 → 考古 → 获得遗物 → 装入召唤器
    → 准备灵魂介质 → 召唤英灵 → 战斗 → 升级与继续探索
```

## 2. 当前设计

### 2.1 英灵回声定位器（回声罗盘）

- 可由玩家制作，或在村庄等地点发现。
- 玩家携带时，可以感知附近包含英灵遗物的考古目标。
- 通过轻灵音效提供距离反馈：越接近目标，提示越明显。
- 实现时避免逐 tick 大范围扫描方块；应使用低频检测、已加载区块限制和目标位置缓存。

### 2.2 英灵遗物

- 主要通过刷取可疑的沙子等考古目标获得。
- 遗物决定可以召唤的英灵类型。
- 当前开发版已注册不可堆叠的 `roman_legionary_relic`（罗马军团战士遗物），使用人工绘制的 16×16 图标，并加入“英灵回声”创造模式标签页，用于打通召唤器 GUI 的遗物装卸测试。
- 后期可以增加随机词条，使同一种遗物仍有重复寻找的价值。

候选词条：

- 坏脾气：召唤成本提高 20%，攻击力增加 4 点。
- 慵懒：召唤成本降低 20%，移动速度降低 25%。
- 勇气：攻击力增加 2 点。
- 瘦削：生命值降低 25%，移动速度和攻击频率各提高 25%。
- 壮硕：护甲增加 4 点，移动速度降低 25%。
- 平凡：没有效果；首版暂不启用，仅保留为未来可能使用的美术占位方案。

召唤器开发版现已提前实现遗物随机天赋及其 GUI/属性闭环；它仍不应阻塞三英灵、考古和回声罗盘等比赛 MVP 主线。

### 2.3 英灵之魂召唤器

- 可升级、可装入遗物的召唤装置。
- 召唤器保持为便携物品；玩家从手持召唤器物品直接打开管理 GUI，不要求先将其放置为方块。
- 手持召唤器时，普通右键用于快速召唤或召回：英灵未出现时召唤，已经出现时将其召回玩家附近；潜行右键打开召唤器 GUI。主动遣散改为 GUI 内的防误触操作，不再占用潜行右键。
- GUI 打开期间锁定用于打开界面的那一个召唤器物品实例所在槽位，禁止拾取、Shift 移动、数字键交换或丢弃该物品，其他玩家物品栏槽位保持正常操作；关闭 GUI 后解除锁定。若该召唤器实例因异常状态不再存在或无法验证，客户端界面应自动关闭并停止继续操作。
- GUI 内的“收回/遣散英灵”操作需要两次点击确认：首次点击进入约 3 秒的确认状态，第二次点击才执行，超时或离开页面则取消。具体颜色、文字和动画可调整。普通右键召回已在场英灵不消耗燃料；主动遣散后再次召唤视为新召唤并重新支付燃料。
- GUI 使用同一个状态按钮承载召唤与收回：英灵未在场时显示“召唤英灵”，点击后执行与普通右键首次召唤相同的燃料检查和召唤逻辑；没有遗物或燃料不足时禁用，并在悬浮说明中给出原因。英灵已在场时按钮切换为“收回英灵”，执行上述两次点击确认的主动遣散。这里的“收回”表示使英灵离场，普通右键“召回”仅表示把仍在场的英灵带回玩家附近。
- 在 GUI 内成功召唤或收回英灵后，界面保持开启并立即刷新在场状态、生命值、燃料和按钮文字；失败时同样保持开启并显示原因。只有玩家主动关闭、召唤器实例失效或菜单验证失败等情况才关闭 GUI。
- 召唤、收回或槽位操作失败时，不向聊天栏重复刷消息；GUI 在相关按钮附近短暂显示约 3 秒的明确原因，例如缺少多少燃料、未装入遗物、找不到安全召唤位置或当前状态不允许操作，并在按钮悬浮说明中保留可查询的原因。具体颜色与位置属于可调整的视觉规范。
- 未装入英灵遗物时仍可打开召唤器 GUI；界面保留遗物槽、操作提示和玩家物品栏，角色形象、名称、技能、属性及相关控制区域显示为未激活状态。装入遗物后立即载入对应英灵数据；取出遗物时立即取消录入、清空相关显示并遣散该召唤器当前召出的英灵。
- 召唤器不再在 GUI 内显示“英灵之魂召唤器”或“[英灵名字]召唤器”标题；GUI 最上方左侧直接显示当前英灵名称，未装入遗物时该区域为空或显示未录入状态。物品栏中的召唤器悬浮名称仍在未装入遗物时显示“英灵之魂召唤器”，装入遗物后动态显示“[英灵名字]召唤器”，例如“罗马军团兵召唤器”，以便玩家区分多个召唤器。底层仍使用同一种召唤器物品类型。
- 玩家通过铁砧设置的召唤器自定义名称优先于物品栏中的默认/动态名称，并在更换或取出遗物后保持不变。GUI 顶部仍显示英灵名称而非召唤器自定义名称；英灵名称超出固定宽度时省略显示，悬浮可查看完整名称。
- GUI 的角色形象窗口实时渲染所录入英灵的 3D 模型，并播放低干扰的待机动画；模型默认朝向玩家，头部和身体在有限角度内小幅跟随鼠标，不自动持续旋转，首版不提供鼠标拖拽旋转。不为该窗口另行要求每个英灵制作静态 2D 肖像，具体转动幅度与视觉反馈可后续调整。
- GUI 以 16×16 图标展示英灵技能，并通过悬浮提示显示详细说明。已占用的技能槽使用美术稿中带白色内框的技能底板，空技能槽使用美术稿中 `#C6C6C6` 内框的空槽底板；两套底板均直接从新版 ASE 对应图层裁切使用，不根据色号重新绘制。普通攻击、跟随和基础防御等核心能力不可禁用；只有明确标记为可选的特殊技能允许玩家点击启用或禁用。技能禁用时保留“已占用”底板，通过图标变暗和禁用遮罩区分，不得显示成空槽。
- GUI 主页面最多预留五个技能图标位置，并采用单排显示；只绘制英灵实际拥有的技能，未装入遗物时五个位置为空。若未来单个英灵超过五项技能，再通过技能区域分页或详细界面承载。
- 英灵遗物词条/天赋在该系统实现后，以 `11×11` 图标直接显示在 GUI 顶部英灵名称栏最右侧，悬浮时显示准确名称、完整效果和数值。每件遗物随机拥有 0～4 个天赋，数量概率依次为：0 个 10%、1 个 25%、2 个 40%、3 个 20%、4 个 5%。创造模式物品栏中的展示遗物保持未鉴定状态；玩家真正取出并放入自身物品栏后，服务端才为该物品实例独立生成并保存天赋。标题栏始终为最多四个图标保留固定空间，实际有几个就显示几个，并从右向左排列；没有天赋时不显示图标。英灵名称只能使用左侧固定区域，过长时省略并通过悬浮显示完整名称。
- 同一天赋不得重复生成；“坏脾气”与“慵懒”互斥，“瘦削”与“壮硕”互斥，其他天赋可自由组合。GUI 按固定天赋顺序排列，不采用随机抽取顺序。“平凡”首版不启用。GUI 先支持 0～4 个动态图标，但随机生成逻辑仍按开发进度安排，不改变随机词条属于 MVP 后内容的范围约束。
- GUI 主页面显示八项主要属性，严格按照新版美术稿从左到右、从上到下排列：生命值/最大生命值、等级、攻击力、攻击速度、护甲、移动速度、警戒范围、召唤消耗比例。常驻文本采用紧凑短格式，示例依次为 `30/60`、`12`、`8`、`125%`、`8`、`100%`、`16`、`120%`；单位、攻击间隔换算和详细定义放入悬浮说明。召唤持续时间不再作为属性。跟随距离、索敌技术参数、击退抗性等次要或 AI 参数放入悬浮说明或后续详细信息区域。
- 英灵已召唤时，GUI 实时显示当前生命值/最大生命值，并同步伤害、自然恢复和最大生命变化；英灵未召唤或死亡离场时显示最大生命值并明确标注“未召唤”，不显示额外死亡冷却。
- GUI 首版采用单页面布局，不设置标签页；最终界面尺寸严格采用新版 `英灵召唤器6.ase` 的 `241×201` GUI 主体，并完全遵循该 ASE 中各元素的实际像素坐标，不做自动对齐或擅自增减一像素。英灵概览、六个模块槽、模式设置、遗物槽、燃料输入槽、燃料条和玩家物品栏同时显示；ASE 主体以外摆放的按钮状态和填充素材仅作为裁切素材，不属于实际界面区域。若未来增加由模块提供的背包或大量详细信息，再按实际需要新增扩展页面。
- 经验条的背景与填充区域严格为 `160×2`，燃料条严格为 `54×3`，不得为了视觉对齐改为其他高度。燃料条底图使用美术稿中的 `#8B8B8B` 区域；填充层底部平铺灵魂沙纹理，并在当前燃料比例形成的裁切遮罩内播放多层、错位、稳定伪随机循环的灵魂粒子效果，避免逐帧完全随机造成闪烁。
- 英灵的“闲逛”活动模式以切换该模式时的位置为锚点，默认只在锚点周围约 16 格内自由行动；不跟随主人，也不因主人远离而传送，战斗或其他临时行为结束后返回该活动区域。
- 英灵的“等待”活动模式以切换该模式时的位置为等待锚点；平时只在锚点周围约 2 格内轻微移动、转头和播放待机行为，不跟随主人也不因距离传送。被推离或临时行为结束后应返回锚点，战斗追击范围由警戒规则另行限制。
- 活动模式约束战斗追击边界：跟随模式围绕主人作战并沿用跟随/超距传送规则；等待模式主动扫描锚点约 6 格内的目标，响应与追击边界为锚点约 8 格，切换或重复选择等待模式时立即清除旧目标并重设锚点；闲逛模式在 16 格活动区内发现目标，最多追至距锚点约 24 格后放弃并返回。和平警戒下不会因发现目标主动追击。上述距离为初始值，可依据寻路与战斗测试微调。
- 索敌、追击和实际近战命中距离是彼此独立的规则。军团兵可以在警戒范围内发现并追赶目标，但普通挥刀只有在伤害判定帧仍满足原版实体近战范围且与目标之间有视线时才会造成伤害；目标在攻击起手后离开范围或躲到障碍物后方时不得隔空受伤。
- 警戒状态分为三种：主动出击会主动攻击允许范围内的敌对生物，并响应主人正在攻击的目标；被动防御只攻击伤害主人、自身或被主人攻击的目标；和平模式不主动攻击任何目标，但英灵自身受到直接攻击后仍可自卫。新录入遗物默认使用“跟随”活动模式与“被动防御”警戒状态。
- 英灵不使用与玩家相同的盔甲栏和可替换武器栏；GUI 不为普通英灵装备预留槽位，构筑与强化统一由可放入、可取出的召唤器升级模块承担。
- 召唤器提供六个完全开放的升级模块槽，模块可自由放入和取出。六槽的具体排列不锁定为 2×3，可在实际 GUI 排版和开发测试中调整而不改变玩法规则。
- 升级模块默认允许重复安装并简单叠加，数值按最多占用六槽进行平衡。需要禁止叠加的特殊模块由模块自身的名称/悬浮说明明确标注，并由服务端槽位规则拒绝重复安装；GUI 不常驻显示全局“不叠加”规则，但在无效放入时提供清晰反馈。
- 基础英灵不自带背包，当前 GUI 不预留常驻英灵物品栏。未来可由占用升级槽的“携行”类模块提供少量储物格；届时必须单独定义英灵死亡、收回、遗物取出、召唤器转交及模块拆除时的物品去向和所有权规则。
- 英灵具有有限成长系统，等级上限为 30 级；GUI 显示当前等级和经验进度。等级与经验绑定并保存在英灵遗物上，遗物被取出或换入其他召唤器时完整保留成长；召唤器负责燃料和模块。以军团兵的 30 点基础生命和 6 点基础攻击为基准：2～29 级每级增加 1 点最大生命，30 级增加 2 点，使满级精确达到 60 点生命；在 5、10、15、20、25、30 级各增加 1 点攻击力，使满级精确达到 12 点攻击。其他英灵可按自身基础值采用对应的整数里程碑成长表；护甲、移速和攻击频率暂不随等级提升。升至下一级所需成长经验暂按 `15 + 2 × 当前等级` 计算，从 1 级升至 30 级累计需要 1305 点成长经验，后续可依据实测节奏调参。
- 活动模式、警戒状态和可选技能启用/禁用状态跟随英灵遗物保存；遗物换入其他召唤器后保留这些设置。新遗物默认使用“跟随＋被动防御”，可选技能默认开启。等待和闲逛模式的具体位置锚点只属于当前召出的实体，不随遗物跨召唤保存；燃料与升级模块仍属于召唤器。
- 英灵遗物通过共同战斗获得独立成长经验：英灵击杀敌对生物，或主人击杀英灵近期参与战斗的敌对生物时，遗物都可获得经验。参与可由近期造成伤害、承受目标攻击或锁定该目标判定，避免坦克和防御型英灵必须争夺最后一击。该成长经验不拾取、不分走也不减少玩家获得的原版经验球，经验量参考敌人的原版经验价值。
- 当前开发版已实现上述经验闭环：参与窗口为 10 秒，同一主人有多个符合条件的英灵时平均分配目标的原版经验值；等级和当前级经验保存在遗物上，升级会立即刷新在场英灵的生命与攻击属性，并以行动栏文字和玩家升级提示音反馈。满级后不再累计溢出经验。
- 士兵阵列是可在 GUI 技能栏手动启用或禁用的常驻光环，不要求附近存在敌人。实际增益通过无限持续时间的自定义状态效果实现：默认显示“举起武器！”并固定提供 +2 攻击；主人任一只手持盾时立即切换为“举起盾牌！”，继续提供同一份 +2 攻击，并额外驱动主人和同主人军团兵的 15% 最终伤害减免。两种状态各自使用独立的 18×18 图标。受益者离开 8 格范围、技能关闭、来源消失或玩家断开连接时直接移除效果，玩家重新连接时还会清理旧版本遗留的无限效果，避免永久残留和短持续时间闪烁；不再使用范围环粒子作为主要判定依据。GUI 中士兵阵列技能始终使用普通白边技能框，不以黄色外框表示生效。
- 消耗灵魂介质产生召唤燃料；每次召唤支付一定燃料，英灵自然恢复生命时持续消耗少量燃料。属性中的“召唤消耗比例”同时修正一次召唤成本和自然生命恢复的燃料成本，默认值为 100%；所有天赋、模块等修正后的最终比例显示在 GUI 中。
- 英灵存活期间没有持续维持燃料消耗。燃料耗尽不会使在场英灵消散，也不妨碍其正常战斗，但会停止需要燃料的自然生命恢复；燃料只在实际召唤和实际恢复生命时扣除。
- 英灵仅在脱离战斗约 5 秒后开始自然恢复生命；受到攻击、重新锁定攻击目标或再次参与战斗时立即停止恢复。恢复按实际补回的生命值和当前召唤消耗比例扣除燃料，满血或未实际恢复时不扣燃料，燃料不足则暂停恢复。比例修正产生的小数燃料成本使用内部定点累计，累计到可扣除整数时再实际扣除，不在每次恢复时粗暴取整。具体恢复间隔和每点生命的基础燃料成本继续通过数值设计与实测确定。
- GUI 提供一个燃料输入槽。有效燃料放入后自动转化为召唤器内部燃料；达到容量上限时停止转化，未消耗的剩余物品保留在槽内并可取回，无效物品不能放入。当前数值为：内部燃料容量 1000，灵魂沙提供 50，腐肉提供 20，每次召唤消耗 100，自然恢复每 2 秒恢复 1 点生命且每点消耗 2 燃料。燃料条悬浮时显示当前值、最大值和单次召唤所需燃料；若剩余容量不足以容纳一个燃料物品的完整价值，则暂不消耗该物品，避免燃料溢出浪费。上述数值允许依据实测节奏调整。
- 灵魂沙效率较高，腐肉效率较低。
- 英灵召唤后不再因持续时间自然消散；只在玩家主动收回/遣散、英灵死亡、遗物被取出或绑定失效等明确条件下离场，并以声音和粒子提供反馈。
- 英灵死亡后不设置额外复活/重构冷却；只要遗物仍有效且召唤器燃料足够，玩家可以立即再次召唤，并正常支付单次召唤燃料。死亡不清除遗物上的等级、经验或词条，也不影响召唤器中的模块。

当前召唤器开发实现状态（2026-08-13）：

- 已接入遗物 0～4 个随机天赋、互斥规则、一次性初始化、复制持久化和开发用重掷命令。
- 已接入 1000 容量燃料、腐肉/灵魂沙转换、安全召唤、自然恢复消耗及 GUI 实时显示与转换粒子。
- 已接入跟随/等待/闲逛与主动/被动/和平模式的遗物持久化和基础活动边界。
- 军团兵三项技能已有可运行首版：士兵阵列、举盾冲锋、军团永存；举盾姿态由独立的服务端同步状态和 GeckoLib 姿态控制器维持，举盾冲锋结束或军团永存完成 5 秒蓄力并释放后才播放收盾。举盾期间渲染层完整抵消上半身举盾姿态传递给头部的父骨骼旋转，使头部保持笔直并朝向实体正前方；瞳孔仍可独立追踪周围目标。举盾冲锋使用正弦速度曲线形成“平稳起步—中段加速—末段收势”，沿途播放低频风弹掠过声和少量尾流，命中投射物或苦力怕时统一播放盾牌撞击声。命中或取消后水平速度立即衰减至 15%，只保留短暂惯性；0.5 秒内置冷却从冲锋结束时重新计算，已命中的同一目标另有 2 秒重复拦截保护，避免对仍在起爆的同一只苦力怕连续消耗多次充能。军团永存蓄力期间在自身头顶零星生成沿抛物线先上升后回落的生气粒子，以此取代触发时脚下的风爆反馈；释放声暂用风弹爆发音效。具体范围、路径、击退、粒子和声音仍需实机手感测试后调参。
- 六个升级模块槽本批次明确保持未实装状态：拒绝放入新物品，但允许取出旧存量物品。
- 手工测试项目记录于 `docs/SUMMONER_TEST_CHECKLIST.md`；可交互版本为 `docs/SUMMONER_TEST_CHECKLIST.html`。

第一阶段升级：

- 正式模块目录、名称、数值、设计和图标由后续设计稿确定，不再把生命、攻击和持续时间三项视为已锁定内容。
- 若开发六槽装卸、叠加和属性刷新需要最小案例，可临时使用“生命提高、攻击提高、燃料容量提高”三个占位模块；它们不代表最终模块方案，未经再次确认不得当作正式内容扩展。

后期候选升级：

- 玩家受到攻击时自动召唤。
- 阻挡致命伤害并召唤英灵，带冷却时间。
- 玩家受到的部分伤害由英灵承担。

### 2.4 英灵

比赛版目标为 3 个差异明显的英灵，完成后再考虑扩展到 5 个。

- 军团兵：持盾近战、防护和吸引敌人。
- 骑士：冲锋、拦截和较高爆发伤害。
- 阵线步枪兵：保持距离、缓慢装填和高单次远程伤害。

每个英灵需要具备：

- 独立外观和轮廓。
- 明确的战斗定位。
- 召唤、攻击和消散反馈。
- 稳定的跟随、传送和目标选择行为。

## 3. 比赛版范围

### 3.1 第一阶段：可游玩的军团兵原型

第一阶段只验证“召唤器—英灵”核心闭环，不等待正式考古系统、GUI、能量或完整动画：

1. 独立的 Echo Warrior 创造模式标签页，召唤器作为图标。
2. 不可堆叠的 `test_echo_summoner`，每个物品实例持有唯一标识。
3. 每个召唤器最多绑定一个 `roman_legionary_echo`；多个召唤器可以各召唤一个英灵。
4. 普通右键快速切换召唤状态：没有已召唤英灵时在玩家前方召唤，已有已召唤英灵时主动遣散。
5. 潜行右键打开召唤器 GUI。
6. 英灵跟随召唤者，只在召唤者攻击、召唤者受击或自身受击后反击，不主动扫描并攻击普通生物。主人距离超过 5 格时开始跟随，进入 5 格内停止；超过 32 格才使用安全传送恢复。
7. 英灵召唤后持续存在，不再使用 120 秒自然消散。召唤器离开原持有者物品栏后有 5 秒粘滞时间；宽限超时、进入容器或转交他人均解除旧绑定并使英灵离场。
8. 玩家登出或跨维度时遣散，回到游戏后由玩家手动再次召唤。
9. 使用军团兵模型师交付的待机、行走、攻击和受伤动画；召唤与消散暂时使用原版粒子及声音反馈。举盾和收盾动画已导入，但在盾牌技能确定前只允许通过开发指令预览。

第一阶段明确不包含：考古、回声罗盘、遗物词条、召唤能量、升级、GUI、自动召唤、致命伤保护、伤害分担、跨维度跟随及额外英灵。

MVP 必须先完成：

1. 一个回声罗盘。
2. 一套可重复测试的考古获取流程。
3. 一个召唤器和一种基础灵魂能量流程。
4. 三个可正常跟随和战斗的英灵。
5. 三项简单升级。
6. 完整的声音、粒子、模型、材质和基础英文文本。

以下内容只有在 MVP 稳定后才开始：

- 遗物随机词条。
- 自动召唤。
- 致命伤害保护。
- 伤害分摊。
- 复杂模块 GUI。
- 第四、第五个英灵。

## 4. 技术基线（已冻结）

- 目标游戏版本：Minecraft Java Edition 26.1.2。
- 加载器：Fabric Loader 0.19.3。
- Fabric API：0.155.2+26.1.2。
- Java：25，项目私有运行时位于 `.toolchains/jdk-25`，不修改系统 `JAVA_HOME`。
- SmartBrainLib：2.0.0，必需前置。
- GeckoLib：5.5.2，必需前置。
- 构建：仓库内 Gradle Wrapper，不依赖系统全局 Gradle。
- IDE：IntelliJ IDEA 2025.3 或更高版本。
- 版本控制：Git。
- 模组 ID：`echo_warrior`。
- Java 根包：`com.yuriscat.echowarrior`。

选择 Fabric 的主要理由是开发运行、调试热替换和 26.1 文档路径比较清晰。上述版本均已在 2026-08-07 依据官方项目的 26.1.2 分支或发布信息核对并冻结；升级必须作为单独任务处理。

### 4.1 实体 AI 与依赖策略

英灵需要共享但可组合的感知、记忆和行为，例如：

- 记住召唤者并维持合理跟随距离。
- 发现正在攻击召唤者的敌人。
- 在跟随、保护、攻击、返回和消散之间切换状态。
- 根据英灵类型组合近战、冲锋、格挡或远程行为。
- 在寻路失败、距离过远或跨越地形时安全返回召唤者。

确定将 SmartBrainLib 作为必需前置，并在其上编写 Echo Warrior 专用的 Sensors、Memories 和 Behaviours。第一阶段允许把严格的物品绑定与生命周期检查放在实体服务逻辑中，但移动、目标和战斗活动应走 SBL brain 行为。

截至 2026-08-07，SmartBrainLib 已提供 Minecraft 26.1.2 的 2.0.0 版本，覆盖 Fabric、Forge 和 NeoForge。集成时仍必须用最小英灵原型验证：

1. 26.1.2 构建与开发客户端可以稳定启动。
2. 文档和 2.0.0 API 足够完成跟随、护主、目标选择及战斗行为。
3. 服务端和单人游戏行为一致。
4. 不会明显妨碍热替换和测试效率。

不在比赛周期内完整重写 SmartBrainLib。若验证失败，则退回原版 Goal 系统，并只实现 Echo Warrior 所需的小型共享 AI 组件。

确定将 GeckoLib 作为英灵模型和动画系统的必需前置。目标为 Minecraft 26.1.2 时，对应可用版本为 GeckoLib 5.5.2，并应选择与最终加载器严格匹配的构建。

不使用 Kotlin for Forge：Echo Warrior 使用 Java 开发，SmartBrainLib 与 GeckoLib 均不要求 Kotlin for Forge。只有未来明确引入 Kotlin 源代码时才重新评估。

当前机器检查结果：

- Git 已安装。
- 默认 Java 是 17，不能作为 26.1 的最终开发 JDK。
- 未安装全局 Gradle；这是正常状态，后续使用 `gradlew.bat`。

## 5. 开发与人工测试约定

### 5.1 一键测试目标

项目最终提供：

```text
scripts/run-test-client.ps1
scripts/run-test-client.bat
scripts/playtest-now.ps1
scripts/playtest-now.bat
```

`run-test-client.bat` 是面向 CMD 和双击启动的入口，并转交给 PowerShell 主脚本；避免 Windows 将 `.ps1` 当作普通文件打开。

`playtest-now.bat` 是日常人工测试的最短入口：编译并启动开发客户端，然后通过 Quick Play 直接进入现有的 `CATTEST` 世界。对应 PowerShell 脚本要求该世界已经存在，避免名称错误时静默停在主菜单。

当用户说“测试”“启动 MC”“进测试世界”等表达时，Codex 应：

1. 检查是否已有开发客户端在运行，避免重复启动。
2. 编译当前代码并报告阻塞性错误。
3. 使用 Gradle `runClient` 启动开发客户端。
4. 通过 `--quickPlaySingleplayer` 自动进入固定测试世界。
5. 保留控制台和游戏日志，崩溃后主动定位原因。

一键测试脚本会在本项目 Minecraft 窗口创建完成后尝试恢复窗口并置于前台，避免游戏已经锁定鼠标但窗口仍被其他程序遮挡；该观察器仅在启动阶段短时运行，不常驻后台。

当前固定测试世界目录名：

```text
CATTEST
```

首次需要人工创建世界时，只做一次。之后启动脚本应直接进入该世界。

### 5.2 测试世界原则

- 测试世界只用于开发，不作为正式展示存档。
- 默认开启作弊，方便传送、切换模式和获取物品。
- 建立固定测试区域：考古区、召唤区、战斗区和性能测试区。
- 需要可重复场景时，优先添加开发命令或测试结构，而不是每次人工摆放。
- 对破坏性测试保留单独世界或备份，避免污染主测试世界。

### 5.3 热加载分级

能不重启时，不重启整个客户端：

- 材质、语言、模型和部分客户端资源：优先使用资源重载。
- 配方、战利品表、标签等数据：优先使用 `/reload` 或对应重载流程。
- Java 方法内部逻辑：调试模式下尝试 HotSwap。
- 新增或删除字段、方法、类，以及修改注册表、实体类型、Mixin 结构：通常需要重启客户端。
- 世界生成相关改动：通常需要新建区块；必要时重建测试世界。

如果使用 IntelliJ 调试运行，后续评估 JetBrains Runtime 的增强类重定义参数：

```text
-XX:+AllowEnhancedClassRedefinition
```

它可以提高 Java 热替换覆盖范围，但不能承诺所有模组改动都能安全热加载。

### 5.4 Codex 的测试职责

- 修改代码后先运行与改动规模匹配的检查。
- 能通过自动化测试或 GameTest 验证的行为，不把重复操作推给用户。
- 需要视觉、手感或声音判断时，才请用户进入游戏体验。
- 启动前说明本轮最少需要用户验证的内容。
- 测试后结合 `logs/latest.log` 和崩溃报告继续修复。
- 不以“成功启动”为完成标准；核心场景必须实际可用。

### 5.5 召唤器 GUI 布局调试工具

- 当前召唤器界面已经接入真实服务端容器：六个模块槽、一个燃料槽、一个遗物槽，以及玩家主物品栏和快捷栏均使用原版槽位交互，不再通过客户端手工绘制物品来伪装容器。
- 打开界面的召唤器通过物品实例 UUID 验证并锁定；召唤器内部八个槽位的物品保存在该召唤器物品的数据组件中。物品栈因同步而更换 Java 对象引用时，仍按 UUID 找回当前物品并保存内容。
- GUI 右上角提供临时布局调试入口。调试模式可以选择并拖动十四个逻辑区域；方向键每次移动 1 像素，按住 Shift 时每次移动 5 像素，并提供保存、撤销和重置。
- 布局保存到开发运行目录的 `config/echo_warrior/summoner_layout.json`。槽位坐标在菜单创建时确定，因此保存涉及槽位的布局后会关闭界面，重新打开即可让服务端槽位命中区域与新位置完全一致。
- 当前整张 GUI 底图仍是单张固定贴图。布局工具会实时移动文字、图标、模型、进度填充和槽内物品预览，但不会自动移动已经画死在底图中的边框与底纹；若最终需要大幅改变结构，应按逻辑区域拆分贴图，或依据保存的坐标回到 Aseprite 模板中重新排版。

## 6. 预计项目结构

```text
mod-project/
├─ PROJECT.md
├─ AGENTS.md
├─ README.md
├─ build.gradle
├─ settings.gradle
├─ gradle.properties
├─ gradlew
├─ gradlew.bat
├─ gradle/
├─ scripts/
│  ├─ run-test-client.ps1
│  └─ playtest-now.ps1
├─ src/
│  ├─ main/
│  └─ gametest/
├─ run/
│  ├─ saves/CATTEST/
│  └─ logs/latest.log
└─ docs/
```

`run/`、构建产物和本地测试世界不提交 Git；必要的测试结构、GameTest 和配置则提交。

## 7. 尚未确定

- `Echo Warrior` 是否作为最终发布名称；改展示名不要求更改 `mod_id` 或 Java 包名。
- 第一版遗物的具体考古生成方式。
- 三个英灵的最终美术风格和历史时代。

## 8. 第一阶段固定参数

- 测试召唤器：`test_echo_summoner` / Test Echo Summoner / 测试英灵召唤器。
- 军团兵英灵：`roman_legionary_echo` / Roman Legionary Echo / 罗马军团兵英灵。
- 召唤器动画：`0,1,2,3,4` 循环，每帧 3 tick，总周期 0.75 秒。
- 英灵属性：30 生命、8 护甲、6 攻击、0.28 移速、32 跟随距离、0.3 击退抗性，约 1 秒攻击间隔。
- 跟随参数：距离超过 5 格开始步行跟随，接近至 3 格停止，超过 16 格安全传送；SBL 专用行为每 5 tick 刷新一次原版寻路路径。
- 朝向反馈：首次召唤及手动召回时，英灵的身体和头部均朝向召唤者。
- 视觉生命感：影子、分层注视目标、玩家对视、头部转向、瞳孔追踪与收缩、眉毛下压眨眼及安全状态下的偶发歪头由代码统一接管；瞳孔、头部和身体分别响应目标，战斗 AI 始终保留身体朝向控制权，详细约定见 `docs/VISUAL_BEHAVIOR.md`。
- 军团兵动作：新版待机 3 秒循环、行走 1 秒循环、攻击 1 秒、受伤 0.5 秒；攻击从 SBL 近战行为开始时播放并在第 6 tick 判定伤害，攻击中的受伤不会打断身体攻击动作，但仍会触发代码控制的疼痛闭眼与伤害来源注视。
- 玩家主动对视：看向英灵头部需要持续获取时间，12 格内为 0.5 秒，远距离按线性公式增加；战斗中抑制获取，对视时眼睛、头部和身体分阶段自然转向。完整参数以 `docs/VISUAL_BEHAVIOR.md` 为准。
- 被主人发现偷看后的收尾以短距离走开为主要分支；英灵会先转移瞳孔和头部、再转身行走，并在结束后短暂回避重新注视主人，避免刚移开视线就立刻回头。概率、角度与中断规则以 `docs/VISUAL_BEHAVIOR.md` 为准。
- 碰撞箱：宽 0.75、高 1.95，确保可通过常规一格宽、两格高通道。
- 友军保护：不会伤害主人、同主人英灵、主人驯服生物、同队实体、创造或旁观玩家；主人不能伤害自己的英灵。
