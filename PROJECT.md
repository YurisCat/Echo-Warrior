# CurseForge ModJam 2026 模组项目

> 当前英文名称：`Echo Warrior`
> 名称未来可以调整；首阶段固定技术标识为 `echo_warrior`，Java 根包为 `com.yuriscat.echowarrior`。

## 1. 项目目标

为 CurseForge Minecraft ModJam 2026 制作一个围绕“过去的回声”展开的 Java 模组。

核心体验：玩家通过考古发现英灵遗物，将遗物装入召唤器，短暂召回过去时代的战士幻影，让它们跟随并协助战斗。玩家随后可以培养英灵、改造召唤器并继续寻找更好的遗物。

核心循环：

```text
探索 → 回声定位 → 考古 → 获得遗物 → 装入召唤器
    → 准备灵魂介质 → 召唤英灵 → 战斗 → 升级与继续探索
```

## 2. 当前设计

### 2.1 英灵回声定位器（回声罗盘）

- 回声罗盘不打开额外界面，物品名使用原版金色格式（视觉为橙色 `#FFAA00`）。物品说明写明其会指引玩家前往战场遗迹并提醒携带考古刷子，其中“战场遗迹”为亮白灰色 `#E6E6E6`、“刷子”为浅沙金色 `#EEC39A`；紧接着的第三行以深灰文字提示“右键关闭远途声音提示”，不再插入空行。物品外观按“紫色盘面 → 动态指针 → 高光 → 金属框”分层绘制，并取消开启状态原有的紫色附魔闪光。遗迹外提醒音开启时使用铜框，关闭时使用铁框；32 帧指针会在 2048 格内指向持久化索引中最近的未完成遗迹中心，附近没有目标时持续、确定性地顺时针旋转，不使用原版随机左右摆动。指针纹理保持单套灰度 32 帧，由客户端运行时固定染为青蓝色，不复制彩色帧纹理。指针角度与旋转动画在客户端根据服务端同步的语义状态计算，不再逐刻改写罗盘物品组件，避免动态指针引发手持物品刷新。配方为中央指南针、四个正方向紫水晶碎片和四角铜锭；三级制图师还固定提供 `4 绿宝石 + 1 指南针` 的交易，每次补货最多交易两次。
- 罗盘只读取玩家主手、副手、快捷栏和主物品栏中的实例；收纳袋、潜影盒、地面掉落物和其他容器内的罗盘不工作。玩家在物品栏界面用鼠标暂时拿起罗盘时，服务器仍把它视为玩家持有，以便换槽和拖动不会清除遗迹内清场追踪；真正放入其他容器后才停止工作。普通右键只切换该实例的“遗迹外远途提醒音”，不会关闭指针或遗迹内回响，也不会改动其他罗盘的开关。遗迹内定位或清场模式中，只有另一只手为空时才允许切换；另一只手持有任何物品时，罗盘直接让渡右键，不改变框体状态、不播放切换音也不显示切换消息。遗迹外同样降低罗盘的双持优先级：主手罗盘会让副手物品优先响应，副手罗盘则遵循原版顺序，在主手物品没有处理右键后才会切换。若一只手拿罗盘、另一只手拿原版刷子，且准星正指向可刷取的考古方块，刷子始终优先开始考古。开启时播放升调紫水晶共鸣、切为铜框并提示“你开启了远途回声检测”，关闭时播放降调紫水晶共鸣、切为铁框并提示“你关上了远途回声检测”。
- 仅在主世界工作。首次从 320 格外进入当前目标遗迹的远途回声检测范围时，动作栏提示“回声罗盘好像感应到了什么……”，该提示独立于当前罗盘是否屏蔽提醒音。提示触发后，必须离开检测范围并连续保持至少 5 秒才重新布防；边缘处短暂出入不会反复提示。所有回声罗盘动作栏消息（开启/屏蔽远途声音、首次感应、附近无目标、剩余回声数量、遗迹回声平息）均使用自定义本地化渲染：按 Unicode 字素簇拆分完整句子，各字素以 1 像素幅度独立抖动约 0.65 秒，随后稳定显示并淡出，总时长约 2.4 秒。每次远途或遗迹内语义回声触发时，不再在快捷栏上方重复显示“回声罗盘”；若玩家此时正在物品栏中悬浮查看回声罗盘，则只令悬浮提示框内的物品标题按字素独立抖动约 0.65 秒，抖动刷新频率随接近当前回声目标从约每秒 5 次提高到约每秒 28 次。远途声音被屏蔽后，悬浮标题的视觉脉冲仍保留。遗迹外 320 格内会播放持有者私有、位于玩家自身位置的轻微提醒音，间隔由远处约 12 秒逐渐缩短到近处约 4 秒。若多个实例允许提醒音，每名玩家仍只播放一个，选择优先级为主手、副手、快捷栏从左到右、主物品栏从左到右；所有实例的指针始终正常更新。
- 距离遗迹中心 24 格以内进入遗迹内模式：指针持续顺时针旋转，并随玩家接近当前关键方块由约 1.6 秒一圈加快到约 0.4 秒一圈；位置回响同时按方向、音量和间隔引导目标。遗迹内循声反馈按 48～32、32～20、20～10、10～4、4～0 格形成五个连续阶段，各阶段内部使用平滑曲线过渡而非硬跳档；回响间隔从最远约 3.6 秒逐步缩短到最近约 0.7 秒，恢复到阶段化调整前的最高频率，音量与音高仍按平滑曲线增强。尚未触发的下一次回响会在玩家接近时按新间隔逐刻提前；从遗迹外进入遗迹内时会以距上次回响的已用时间直接套用遗迹内频率，若已经超过新间隔则当刻触发，不再等待遗迹外的旧冷却结束。若回声罗盘正拿在玩家主手或副手，每次远途或遗迹内回响都会短暂显示本地化的“罗盘嗡嗡作响...”并按字素独立抖动；遗迹外根据当前遗迹中心定位，遗迹内改为根据当前关键考古方块定位。文字水平位置根据目标相对镜头的左右方位，在屏幕中心两侧的安全范围内偏移，垂直位置首先根据目标相对视线的上下方向计算，再叠加每次回响的小幅随机漂移。透明度采用两段连续平滑曲线：遗迹外从探测边缘的 10% 提高到遗迹边缘附近的约 50%，进入遗迹内后以 50% 为第二阶段起点，再随接近关键方块提高到 100%；抖动刷新速度仍使用距离响应。仅放在快捷栏或主物品栏中的罗盘不会显示这段方向文字。距离当前回声源方块 24 格以内时，框体开始在铜色与金色之间闪烁，接近时由约每秒 0.45 个完整周期平滑加快到约每秒 1.5 个完整周期，2 格以内稳定为金框；该提示独立于遗迹外提醒音是否被屏蔽，为听觉障碍玩家提供等价的距离反馈。为避免边界抖动，离开 48 格后才回到遗迹外指针模式。罗盘每约 2 秒只查询一次持久化遗迹索引，不扫描方块、不加载区块、不触发遗迹生成，也不缩短补充冷却；2048 格内没有目标时只提示一次“附近没有可响应的战场回声”，并以约 0.5 秒一圈持续顺时针旋转。
- 保底方块消失后，遗迹立即完成并开始冷却。完成瞬间已经锁定该遗迹的玩家会进入个人清场模式：指针保持距离变速的顺时针旋转，回响稳定锁定当前最近的一处剩余考古方块，目标消失后再选下一处；共享剩余数量会立即、变化时及约每 3 秒显示一次，但仅在进入清场模式后的前 10 秒重复，随后只保留用于定位的声音与视觉回响，不再伴随每次回响刷出文字。剩余归零立即释放；若玩家在保底完成后持续 3 秒离开中心 48 格，也会释放并寻找新的活动遗迹。该个人清场归属可在玩家仍携带罗盘且位于范围内时跨登出和服务器重启恢复。

#### 2.1.1 战场遗迹与考古

- 战场遗迹是主世界陆地生态群系中的一小片不规则天然空地，直径约 15～21 格。生成只清理草、花和小型可替换植物，保留原生地表，不砍树；地表高差超过约 1 格、存在结构/方块实体、相关区块被玩家修改或未自然加载时放弃该位置。
- 每个遗迹含 6～9 个外观一致的可疑方块，其中恰好 1 个保底遗物方块，其余为普通考古方块。草地和泥土地表使用稳定的自定义可疑草方块/可疑泥土，沙地和砂砾地表复用原版可疑沙子/可疑砂砾；两个自定义方块物品分别本地化为“可疑的草方块”和“可疑的泥土”，并提示“单独摆放的可疑方块不会刷出任何东西哦！”。每个自定义方块按坐标从五张手绘痕迹中无放回抽取两张，并从十种等权组合与四种等权原版 Y 轴模型朝向中稳定选择一种外观。旋转只复用同一模型，不再预生成旋转纹理；顶部痕迹随模型朝向旋转，侧面纹理保持直立。两层痕迹各以自身透明度乘 50% 的强度依次正片叠底，重叠区域允许自然加深；同一方块的组合和模型朝向在刷取阶段、区块重载与客户端重启后保持一致，不写入额外存档数据。草方块侧面的痕迹向下偏移 2 像素，刷取四阶段叠加原版可疑沙子的渐进磨损差异。生成器保留原版草地纹理的原始灰度采样值，避免色彩空间转换抬高底图亮度；纹理不使用亮色高光，未标记像素保持原版地表颜色，可疑草方块继续使用所在地的生物群系草色。保底方块与普通考古方块采用相同的外观分布，不提供视觉区分。
- 每个战场遗迹在实际放置时从古罗马、墨西加、古埃及、古代中国、日本武家五种文明中等概率绑定一种，并把绑定结果持久化到遗迹状态。保底方块正确刷取时只产出该文明对应的现有英灵遗物；它以非刷子方式消失时不产出遗物，但遗迹仍立即完成。尚未保存文明字段的开发期旧存档继续保留旧战利品表键作为兼容入口：保底方块从五种现有遗物中等概率产出一个，普通方块等概率转交给五套当前文明战利品表，避免已生成的可疑方块刷取为空。后续调整遗物或饰品的文明归属只需修改数据与映射，不需要改写已经保存的知识条目 ID。所有 6～9 个考古位置均持久化登记；刷取、直接破坏、爆炸、替换或下落后从登记位置消失都会更新共享剩余数量，避免罗盘被已不存在的方块永久卡住。
- 普通战场考古使用 `3：3：2：2：1：0.5：2` 的类别权重：铁锭 22.2222%、同文明知识碎片合计 22.2222%、五种传承合计 14.8148%、金锭 14.8148%、同文明额外遗物 7.4074%、下界合金碎片 3.7037%、同文明饰品成品合计 14.8148%。知识碎片只从该遗迹文明的 8 个条目中等权抽取；五种传承彼此等权。饰品按文明与考古掉落档位维护，命中饰品池后尽量按常见 60%、精良 30%、高阶 10% 分配；某文明缺少某一档时，在其现有档位之间按原比例归一化。物品名称使用原版稀有度颜色：常见为白色、精良为黄色、稀有为青色、史诗为紫色；玉璧使用史诗（等级 3），但仍保持原有的高阶 10% 档考古权重。特殊敌人掉落传承属于后续数据驱动扩展，本次只保留物品标签与接口，不绑定具体敌人。
- 主世界按 `48×48` 区块划分考古区域，每区域最多一个活动遗迹，活动遗迹中心至少相距 512 格。保底方块消失后，旧区域仍进入 24000 游戏刻（服务器运行约 20 分钟）的本地复用冷却；同时立即写入一条可执行的持久化异地补位任务，优先在另一大区域、距旧遗迹 640～720 格的位置生成下一处，不设置额外的游戏性等待。前 8 次尝试未找到安全地形时放宽至 512～896 格，持续失败再分阶段放宽至最远 1280 格，但绝不覆盖活动或冷却区域。性能安全由生成调度保证：补位检查每秒至多进行一次，每连续 8 次失败退避约 10 秒，并与全维度约 30 秒至多成功放置一处的总节流共享；它只临时加载候选遗迹所需区块，不留下强加载。因此正常情况下会在完成后的下一次每秒检查中开始尝试，但可能受总节流或安全地形搜索影响。任务在重启后继续，服务器停机期间冷却与调度时间均暂停。旧区域冷却结束后，仍只会在符合条件的区块再次自然加载时尝试普通补充。
- 旧存档中首次自然加载的区域先随机等待约 2～5 分钟，新生成区块使用较短初始化延迟；每个维度实际放置至多约 30 秒一次。区块通过持久化布尔附件记录玩家方块改动，遗迹索引、冷却和近期完成位置保存在维度 SavedData 中，不进行全世界逐 tick 扫描。
- 管理员测试可使用 `/echo_warrior battlefield locate` 查询索引中最近的未完成遗迹及保底方块坐标；`/echo_warrior battlefield teleport` 会传送到遗迹中心外随机 32～50 格的安全位置，便于测试外部指针；`/echo_warrior battlefield generate [数量]` 可强制请求 1～8 处、默认 1 处正式遗迹。生成任务分散到后续服务器刻逐步执行并报告进度，避免批量新区块生成长时间冻结单个指令；它只在玩家 2048 格内选择彼此独立的正式区域，继续遵守 512 格间距、生态群系、地形、结构与玩家改动安全检查，只正常加载验证所需区块，不留下强加载。等待生成的区域会从该区域当前自然加载的区块中轮换候选位置，卸载区块不会继续阻塞整个区域。

#### 2.1.2 知识碎片与知识碎片集

- 运行时只注册一个 `knowledge_fragment` 物品类型，具体页面由物品自定义数据中的稳定条目 ID 决定；相同 ID 可以堆叠至 64，不同 ID 因组件不同不会混堆。首批内容为五种文明各 8 页、共 40 页，按古罗马 → 墨西加 → 古埃及 → 古代中国 → 日本武家的固定顺序显示。正文中英文分别维护，篇幅以英文版约占纸张三分之一到三分之二为目标；史实、传说、神话、考古发现与武器介绍均可采用，但神话和传说必须在正文及来源元数据中明确标识。
- 条目目录位于 `data/echo_warrior/knowledge/entries.json`，物品只保存稳定 ID；标题、正文、来源、证据类型和配图可以更新而无需迁移玩家物品。只有显式标记 `illustration_binding: high`、与条目主题存在强绑定的页面才显示配图；2026-08-30 的饰品史料修订最终以稳定 ID 一对一替换 24 个旧条目，全部绑定对应饰品图标，连同原先保留的 3 个高绑定条目，当前 40 页中共有 27 页显示配图。每页只保留 1 张核心图标，普通书、剑、材料等弱关联占位不再显示。物品配图使用离线导出的稳定 16×16 资源，完整保留原物品纹理的 RGB 与透明轮廓，不应用墨色变暗、运行时滤镜或半透明方块；未悬浮时使用同一原色图标的 50% alpha 版本，悬浮后恢复 100% alpha，并显示 1 像素页面色高亮框和物品名提示。配图位于页面水平中央偏下。中文文明名、标题与正文始终使用默认字号；英文文明名与标题超过正文安全宽度时等比缩小，英文正文只有在配图或页面下缘前放不下时才自动寻找可完整容纳的最大字号，不滚动也不截断。24 页的逐条史实、权威链接和幻想边界记录在 `docs/research/knowledge-fragments-accessories-2026-08-30/report-source.md`。
- 任意知识碎片与知识碎片集可在合成栏中无序合并；为匹配原版每个已占用合成格只消耗一件物品的规则，一次合成至少需要两个已占用格。合并多个合集时，阅读位置继承合成格中按从左到右、从上到下扫描到的第一本合集。单独一本合集不能借此重新合成。玩家也可把手持的一整叠知识碎片左键放入物品栏中的合集；重复页面只增加该 ID 的持有数量，不在翻页序列中重复出现。
- `knowledge_fragment_collection` 不可堆叠且没有容量上限，内部以条目 ID → 数量保存，并记住上次阅读页。普通右键知识碎片或合集打开阅读界面；按关闭按钮、Esc 或 E 均可退出。合集使用中央左右按钮翻页，到达首尾时隐藏对应按钮，不循环翻页；单页重复数量大于 1 时只在角落以不醒目的 `xN` 显示，只有 1 张时不显示。
- 知识碎片与知识碎片集的物品名称统一使用淡纸色 `#FFEDC9`。知识碎片提示依次显示文明与页面标题副标题、右键阅读和“按 Shift 查看更多”；按住 Shift 时展开“与其他知识碎片合成为知识碎片集”及未来回收箱说明。展开文本中的“知识碎片／知识碎片集”继续使用 `#FFEDC9`，“回收箱”使用棕色 `#A68358`，“回收”使用绿色 `#93CE85`。Shift 状态由客户端供应器读取，通用物品类不直接引用客户端类型，保持专用服务器安全。
- 阅读界面沿用原版书本类 GUI 的像素规则：使用 192×197 的居中阅读区域，合集以新版 ASE 合成出的完整 163×193 背景、单页以完整 146×181 背景按 GUI 逻辑像素 1:1 绘制，不做额外缩放或分块拼接；纸张内框、文明与标题保持在屏幕中心线上，正文根据实际可见字形再向右校正 4 个 GUI 逻辑像素，使左右视觉留白接近一致。顶部文明、标题和正文分别下移以留出呼吸空间；文明与标题明确关闭文字阴影。翻页、拆页与关闭按钮保持原始尺寸和相对位置，悬浮时取消矩形色块，只把按钮自身的非透明像素蒙版向右下偏移 1 像素绘制阴影，移出后立即消失。打开页面时世界画面使用原版透明背景遮罩变暗，但底部快捷物品栏继续可见。
- 合集左下角按钮悬浮文字固定为“从册中拆出一张该页”。点击后当前 ID 数量减一并把一张独立碎片放入玩家背包；背包已满时直接掉在玩家脚边。若该 ID 仍有剩余，继续停留当前页；若该页归零，则优先转到规范顺序中的下一页，没有下一页才转到上一页。合集总数由 2 张拆为 1 张时，源槽中的合集立即变成剩余的独立知识碎片，拆出的另一张同时进入背包或掉落，阅读界面关闭；两张相同页面也遵循同一规则。
- 文明中立的额外箱子来源使用“先五种文明等权、再从该文明 8 页中等权”的两阶段抽取，重复允许：制图师村庄箱与简单地牢箱各有 25% 概率获得恰好 1 张，废弃矿井箱有 15% 概率获得恰好 1 张，要塞图书馆箱有 75% 概率获得 1～2 张。Minecraft 26.1.2 的原版村庄图书馆结构没有箱子或专用战利品表，因此当前不能实现“图书馆箱 25%”而又不污染所有普通村民房屋；若未来为图书馆新增容器，再按 25%、恰好 1 张接入。
- 知识碎片与合集已经加入 `echo_warrior:recyclable_knowledge` 物品标签，并接入英灵杂物回收箱；合集按内部碎片总张数逐张回收，不把整本合集视为单次抽取。
- 创造模式新增独立的“知识碎片”标签页，标签图标使用知识碎片集；标签内容首先显示一份全满合集，其中 40 个具体页面各收录 1 张、阅读位置固定为规范顺序第一页，随后再按规范顺序列出全部 40 个独立页面。知识物品不再重复占用“英灵回声”主标签页。

#### 2.1.3 英灵杂物回收箱

- 英灵杂物回收箱用于给重复考古物提供止损出口。它是 27 格单箱，配方为四角铁锭、四边任意木板、中央任意独立知识碎片；不同种类木板可以混用，知识碎片集不能作为中央材料。箱子名称使用 `#419386`，实体使用原版单箱模型与人工提供的专用贴图；界面沿用原版单箱布局，右上角增加只在悬浮时显示说明的提示按钮。
- 交易发生前它在玩家体感上就是普通箱子：任意物品都能放入，也能随时取回。主世界时钟跨过午夜时，已加载的回收箱按坐标分散到后续 0～20 tick 开始一次无选择回收；睡眠跳过午夜同样触发，倒拨时间和反复设置时间不会重复触发。未加载区块不会被强制加载，重新加载后只补做一次，多个错过的午夜合并为一批；新放置或重新放置的箱子不会立即追赶刚刚错过的午夜。
- 回收开始时关闭正在查看箱子的界面，并锁定玩家与漏斗 40 tick；交易结果先完整计算并写入方块实体数据，再原子替换箱内物品。封存期间箱子不可破坏、不可被爆炸摧毁，且始终不能被活塞推动。只有六个 `recycler/*` 数据标签识别的知识、传承、三档饰品回收收益与遗物会被处理，其他物品和已经生成的材料原样保留；稀有与史诗物品共同使用高阶饰品回收收益。
- 每个物品单位独立抽取，不积攒点数：知识碎片每张抽普通池 1 次；合集按内部每张碎片分别抽取；传承抽普通池 1 次并独立有 2% 概率追加稀有池；常见／精良／高阶饰品分别抽普通池 1／1／2 次，并有 5%／10%／20% 概率追加稀有池，其中稀有与史诗均属于高阶回收档。等级为 `L` 的英灵遗物抽普通池 `2 + floor(L / 5)` 次，追加稀有池概率为 `25% + 2% × (L - 1)`，追加超级稀有池概率为 `0.25% + 0.1% × (L - 1)`；只读取 1～30 的整数等级，部分经验、天赋和其他培养状态都会随回收销毁。
- 普通池权重为铁锭 8、皮革 5、金锭 4、绿宝石 3，每次产出 1 个。稀有池权重为钻石 7、恶魂之泪 5、附魔之瓶 4、金苹果 3、下界合金碎片 1；附魔之瓶命中时产出 3 个，其余产出 1 个。超级稀有池权重为下界合金升级锻造模板 3、附魔金苹果 3、下界合金锭 2，每次产出 1 个。三个池均由独立战利品表驱动，后续可只改数据而不改 Java。
- 处理顺序固定为从左到右、从上到下，并尽可能处理最长的安全前缀。某一单位的全部产出无法放入 27 格时，该单位不消耗并停止本批；前面已经能完整放入的单位仍正常提交，不向地面丢物品，也没有隐藏库存。若存在有效输入但一个单位都放不下，箱子仅轻微合盖、播放沉闷声和少量烟雾，玩家下次打开时收到“昨夜因空间不足未能回收，请腾出箱内空间。”
- 成功反馈按本批最高产出档位显示普通、稀有或超级稀有粒子与声音，不发送刷屏文字。管理员可看向 8 格内箱子使用 `/echo_warrior recycler trigger` 立即按正式规则尝试一批而不占用自然午夜资格，也可用 `/echo_warrior recycler effect normal|rare|super|failure` 单独预览四种反馈。

#### 2.1.4 教程手册《回声、战士与你》

- `tutorial_manual` 是不可堆叠的游戏内教程物品，使用任意一张独立知识碎片与一本书无序合成，并加入“英灵回声”创造模式标签页。普通右键打开教程界面；手册把当前页保存在物品自定义数据中，关闭、转交或重新进入世界后继续从上次位置阅读。
- 教程当前固定为 44 页，分为标题、搜寻英灵、回声英灵、英灵饰品、制作人员名单五个大章节。封面和简介分为两页；五位英灵各自拥有一页遗物与只读技能图标说明；25 个英灵饰品根据现有物品效果和正式合成表各占一页；制作人员名单首先居中显示人工提供的静态像素头像与“感谢你的阅读和游玩”，随后为制作名单和两页知识来源。
- GUI 使用 `human-work/非英灵相关/教程手册/教程手册GUI.ase` 更新后的 165×201 纸张与原始像素坐标。章节条带位于纸张左侧后方；整套条带绘制、命中与动画坐标统一右移 1 像素，未选中时缩在纸张后方，其 16×16 图标占位符右缘与纸张左缘严格相隔 1 像素。悬浮时条带连同图标向左缓动 4 像素，当前选中的章节向左移出 6 像素并锁定。前翻与后翻分别使用 ASE 中独立的 15×13 按钮，并严格采用相对纸张 `(2,176)` 与 `(148,176)` 的偏下位置。条带悬浮框显示章节标题和副标题；物品、附件、技能与标签图标悬浮时均不绘制额外边框。前翻、后翻与关闭按钮的悬浮阴影只使用按钮自身非透明像素的蒙版，并向左下偏移 1 像素。配方与正文的最终间距等待实机视觉验收后再调整。
- 配方附件直接按手册 GUI 的 3×3 网格、箭头和输出槽绘制，标签原料每 40 tick 轮换一个代表物品。技能附件悬浮显示说明但不能切换；埃及弓箭手同时展示赞美猫神、叶形箭、锥锋箭、战车与齐射、后撤步五个现有图标，不显示不存在的普通箭图标。
- 教程中文始终保持默认字号。纸张内的英文封面文字、标题、正文、附件提示、饰品稀有度与效果、配方标题等均按各自安全宽高检测溢出：未溢出时保持默认字号，溢出时自动寻找可完整容纳的最大字号。当前知识碎片页约使用 79% 字号，英灵召唤器页约使用 83% 字号。英文制作名单中括号内的补充说明使用默认字号的 `8/9`，并采用紧凑行距；英文感谢语固定分为两行居中显示。
- “你会发现……”页的知识碎片、轮换传承和轮换饰品附件可点击跳转；知识碎片跳到知识页，传承跳到传承页，饰品跳到当时显示的具体饰品页。教程运行时素材由 `scripts/extract-tutorial-manual-assets.py` 从上述 ASE、人工物品图标和人工静态头像导出到资源目录，源文件继续保留在 `human-work/`。

### 2.2 英灵遗物

- 主要通过刷取战场遗迹中的保底可疑方块获得；每个活动遗迹保证存在一个遗物。
- 遗物决定可以召唤的英灵类型。
- 当前开发版已注册不可堆叠的 `roman_legionary_relic`、`aztec_warrior_relic`、`egyptian_archer_relic`、`guandao_warrior_relic` 与 `japanese_samurai_relic`，并加入“英灵回声”创造模式标签页。五种遗物均可装入同一种召唤器，由遗物类型决定生成的英灵。
- 每件已鉴定遗物生成 2～4 个纯正向天赋：2 个 60%、3 个 30%、4 个 10%。所有具体天赋使用相同抽取权重，不设置类别保底或类别配额，同一天赋不会重复；随机组合本身属于构筑乐趣，不提供定向洗练保护。
- 基础天赋：暴脾气在英灵受到可归因的生物伤害后提供 5 秒、不可叠层但可刷新的 15% 伤害；懒惰使召唤和自然恢复燃料成本降低 15%；勇敢提供 1 点固定攻击；苗条提供 10% 移动速度与 10% 攻击速度；健壮提供 2 点护甲。
- 生物杀手：亡灵杀手、节肢杀手、灾厄杀手分别对原版对应实体标签提供 20% 伤害；三者最多出现一个。巨人杀手独立于前三者，对实际最大生命值严格大于 50 点的目标提供 15% 伤害。
- 维度收割者：下界收割者、末地收割者分别在对应维度提供 15% 伤害；异界收割者在主世界之外的所有维度（含下界、末地和模组维度）提供 10% 伤害；三者最多出现一个。生物杀手与维度收割者是独立轴，可以同时生效。
- 环境与状态天赋：生物群系亲和固定等概率生成林地、原野、荒地、寒地、水域或地下亲和，只在主世界按“地下→寒地→水域→荒地→林地→原野兜底”判断，匹配时提供 10% 伤害与 10% 移速；夜猫子仅在有天空光照和自然昼夜的维度、时间 13000～23000 生效，提供 15% 伤害与 10% 移速；完美主义者在严格满血时提供 15% 伤害与 10% 攻速；背水一战按已损失生命线性提高伤害，最多 25%；不屈按已损失生命线性提供最终减伤，最多 30%，与其他减伤乘算。
- 主人支援天赋只在对应英灵已召唤、与主人同维度且相距不超过 32 格时生效；同名天赋不叠加，不同天赋可以共存。口才只作用于普通村民，在打开交易界面时以原版声望、治愈和村庄英雄修正后的当前价格制作会话快照，两个支付项均降低 15%、向下取整且最低 1，产出和村民永久报价不变；睿智使主人拾取经验球和该英灵成长经验提高 25%，先放大经验球再处理经验修补；导师只在主人亲手击杀生物时额外生成 50% 的世界经验球；幸运只在方块战利品计算期间提供临时时运 +1，最高按时运 IV 计算，已有高于 IV 的等级不变且精准采集仍优先；渔获提供临时海之眷顾 +1、最高按 IV 计算，并把等待鱼靠近的时间缩短 20%。
- 所有百分比伤害天赋先相加后统一乘算；勇敢的固定攻击先加入基础伤害，饰品暴击等独立系统在天赋倍率之后结算。移动速度与攻击速度的百分比加成分别相加，不另设超出原版属性与动作最小间隔之外的硬上限。苗条与健壮互斥；暴脾气与懒惰允许共存；睿智与导师也允许共存。

召唤器开发版已经实现 22 项遗物随机天赋及其 GUI、战斗、燃料、成长、采掘、钓鱼和交易闭环。当前开发存档允许直接重建，不为旧五天赋测试数据添加自动迁移层。

### 2.3 英灵之魂召唤器

- 可升级、可装入遗物的召唤装置。
- 正式合成表为：第一行铁锭、玻璃板、铁锭；第二行铁锭、末影珍珠、铁锭；第三行铁锭、箱子、铁锭。
- 召唤器保持为便携物品；玩家从手持召唤器物品直接打开管理 GUI，不要求先将其放置为方块。
- 召唤器物品与“英灵回声”创造模式标签页统一使用人工提供的 `16×16` 灰色单帧图标，不再播放序列帧。物品处于 GUI 图标渲染时，按住 Shift 会把已装入遗物的召唤器临时显示为该遗物图标；松开 Shift 立即恢复召唤器图标，未装入遗物时保持不变。
- 鼠标拿起有效燃料、英灵遗物或饰品后，可直接对玩家物品栏中的召唤器左键放入；召唤器 GUI 已开启时，也可以对界面内仍处于锁定状态的召唤器物品槽执行同样操作。燃料先进入内部燃料输入槽并按原有节奏逐个转化，遗物只进入空遗物槽，饰品只进入首个兼容空槽；槽位被占用、燃料种类不兼容或物品无效时不改变鼠标物品。该交互遵循原版收纳袋式的“物品点容器”操作，不把燃料瞬间折算成内部数值。
- 成功直接装入燃料时，从召唤器物品格中心生成与燃料类型一致的屏幕粒子：腐肉为棕红色，灵魂沙与灵魂土为青蓝色；每个粒子以随机方向和随机距离爆出到当前 `16×16` 物品格之外，再平滑聚回召唤器。成功直接装入英灵遗物或饰品时，一道较宽、较慢且带过冲缓动的白色斜光条立即从物品格上方扫向右下角并淡出；光条逐像素读取召唤器纹理透明度，只覆盖图标非全透明的区域，透明背景不得被照亮。失败填充不播放对应反馈。
- 手持召唤器时，普通右键打开召唤器 GUI；潜行右键用于快速召唤或召回：英灵未出现时召唤，已经出现时将其召回玩家附近的可站立安全落点；附近没有安全落点时不把英灵传送进空气或方块。主动遣散保留为 GUI 内的防误触操作。
- GUI 打开期间锁定用于打开界面的那一个召唤器物品实例所在槽位，禁止拾取、Shift 移动、数字键交换或丢弃该物品，但允许鼠标拿着有效燃料、英灵遗物或饰品对该槽左键，将物品快速投放进召唤器内部；其他玩家物品栏槽位保持正常操作，关闭 GUI 后解除锁定。若该召唤器实例因异常状态不再存在或无法验证，客户端界面应自动关闭并停止继续操作。
- GUI 内的“收回/遣散英灵”改为单击立即执行，不再要求二次确认。潜行右键召回已在场英灵不消耗燃料；主动遣散后再次召唤视为新召唤并重新支付燃料。
- GUI 使用同一个状态按钮承载召唤与收回：英灵未在场时显示“召唤英灵”，点击后执行与潜行右键首次召唤相同的燃料检查和召唤逻辑；没有遗物或燃料不足时禁用，并在悬浮说明中给出原因。英灵已在场时按钮切换为“收回英灵”，单击后主动遣散。这里的“收回”表示使英灵离场，潜行右键“召回”仅表示把仍在场的英灵带回玩家附近。
- GUI 收到服务端成功结果后才播放按钮局部反馈：召唤时青蓝色灵魂粒子从矩形按钮内部爆向按钮四周，使用随机边缘终点、轻微过冲、交错延迟与摆动轨迹四散；收回时蓝色粒子从按钮四周先短促反向蓄力，再加速聚入矩形按钮内部的不同位置并消失。两种动画均按局部范围、局部辉光和局部尺寸脉冲增强缓动质感，不得扩展成全屏效果；世界中的召唤/离场粒子与声音保持不变。
- 在 GUI 内成功召唤或收回英灵后，界面保持开启并立即刷新在场状态、生命值、燃料和按钮文字；失败时同样保持开启并显示原因。只有玩家主动关闭、召唤器实例失效或菜单验证失败等情况才关闭 GUI。
- 召唤、收回或槽位操作失败时，不向聊天栏重复刷消息；GUI 在相关按钮附近短暂显示约 3 秒的明确原因，例如缺少多少燃料、未装入遗物、找不到安全召唤位置或当前状态不允许操作，并在按钮悬浮说明中保留可查询的原因。具体颜色与位置属于可调整的视觉规范。
- 未装入英灵遗物时仍可打开召唤器 GUI；界面保留遗物槽、操作提示和玩家物品栏，角色形象、名称、技能、属性及相关控制区域显示为未激活状态。装入遗物后立即载入对应英灵数据；取出遗物时立即取消录入、清空相关显示并遣散该召唤器当前召出的英灵。
- 召唤器不再在 GUI 内显示“英灵之魂召唤器”或“[英灵名字]召唤器”标题；GUI 最上方左侧直接显示当前英灵名称，未装入遗物时该区域为空或显示未录入状态。物品栏中的召唤器悬浮名称仍在未装入遗物时显示“英灵之魂召唤器”，装入遗物后动态显示“[英灵名字]召唤器”，例如“罗马军团兵召唤器”，以便玩家区分多个召唤器。底层仍使用同一种召唤器物品类型。
- 玩家通过铁砧设置的召唤器自定义名称优先于物品栏中的默认/动态名称，并在更换或取出遗物后保持不变。GUI 顶部仍显示英灵名称而非召唤器自定义名称；英灵名称超出固定宽度时省略显示，悬浮可查看完整名称。
- GUI 的角色形象窗口实时渲染所录入英灵的 3D 模型，并播放低干扰的待机动画；模型默认朝向玩家，头部和身体在有限角度内小幅跟随鼠标，不自动持续旋转，首版不提供鼠标拖拽旋转。不为该窗口另行要求每个英灵制作静态 2D 肖像，具体转动幅度与视觉反馈可后续调整。
- GUI 以 16×16 图标展示英灵技能，并通过悬浮提示显示详细说明。已占用的技能槽使用美术稿中带白色内框的技能底板，空技能槽使用美术稿中 `#C6C6C6` 内框的空槽底板；两套底板均直接从新版 ASE 对应图层裁切使用，不根据色号重新绘制。普通攻击、跟随和基础防御等核心能力不可禁用；只有明确标记为可选的特殊技能允许玩家点击启用或禁用。技能禁用时保留“已占用”底板，通过图标变暗和禁用遮罩区分，不得显示成空槽。
- GUI 主页面最多预留五个技能图标位置，并采用单排显示；只绘制英灵实际拥有的技能，未装入遗物时五个位置为空。若未来单个英灵超过五项技能，再通过技能区域分页或详细界面承载。
- 英灵遗物天赋以 `11×11` 图标直接显示在 GUI 顶部英灵名称栏最右侧，悬浮时显示本地化的名称与精简效果说明。每件遗物随机拥有 2～4 个天赋，数量概率为 2 个 60%、3 个 30%、4 个 10%。创造模式物品栏中的展示遗物保持未鉴定状态；玩家真正取出并放入自身物品栏后，服务端才为该物品实例独立生成并保存天赋。标题栏始终为最多四个图标保留固定空间，实际有几个就显示几个，并从右向左排列；生物群系亲和根据遗物固定的亲和类别显示对应的六种自制图标和名称。
- 同一天赋不得重复生成；亡灵／节肢／灾厄杀手三选一，下界／末地／异界收割者三选一，苗条与健壮互斥，其他天赋可自由组合。GUI 按固定天赋顺序排列，不采用随机抽取顺序；当前不生成无效果的“平凡”天赋。
- GUI 主页面显示八项主要属性，严格按照新版美术稿从左到右、从上到下排列：生命值/最大生命值、等级、攻击力、攻击速度、护甲、移动速度、警戒范围、召唤消耗比例。常驻文本采用紧凑短格式，示例依次为 `30/60`、`12`、`8`、`125%`、`8`、`100%`、`16`、`120%`；单位、攻击间隔换算和详细定义放入悬浮说明。召唤持续时间不再作为属性。跟随距离、索敌技术参数、击退抗性等次要或 AI 参数放入悬浮说明或后续详细信息区域。
- 英灵已召唤时，GUI 实时显示当前生命值/最大生命值，并同步伤害、自然恢复和最大生命变化；英灵未召唤或死亡离场时显示最大生命值并明确标注“未召唤”，不显示额外死亡冷却。
- GUI 首版采用单页面布局，不设置标签页；最终界面尺寸严格采用新版 `英灵召唤器6.ase` 的 `241×201` GUI 主体，并完全遵循该 ASE 中各元素的实际像素坐标，不做自动对齐或擅自增减一像素。英灵概览、六个饰品槽、模式设置、遗物槽、燃料输入槽、燃料条和玩家物品栏同时显示；ASE 主体以外摆放的按钮状态和填充素材仅作为裁切素材，不属于实际界面区域。若未来增加由饰品提供的背包或大量详细信息，再按实际需要新增扩展页面。
- 经验条的背景与填充区域严格为 `160×2`，燃料条严格为 `54×3`，不得为了视觉对齐改为其他高度。燃料条底图使用美术稿中的 `#8B8B8B` 区域；填充层底部平铺灵魂沙纹理，并在当前燃料比例形成的裁切遮罩内播放多层、错位、稳定伪随机循环的灵魂粒子效果，避免逐帧完全随机造成闪烁。
- 英灵的“闲逛”活动模式以切换该模式时的位置为锚点，默认只在锚点周围约 16 格内自由行动；不跟随主人，也不因主人远离而传送。闲逛采用共用的“停留—漫步—归位”状态机：通常停留 2～5 秒后选择可到达的安全陆地位置，单次移动以约 4～8 格为主并偶尔延长，接近约 12～14 格软边界时偏向锚点方向，越过 16 格硬边界或战斗结束后自动归位。无效目标、寻路失败和卡住状态会重新选点，不再出现路径刚开始便被跟随行为取消的情况。
- 英灵的“等待”活动模式以切换该模式时的位置为等待锚点；平时只在锚点周围约 2 格内停留、转头和播放待机行为，不跟随主人也不因距离传送。被推离或临时行为结束后会通过同一套共用活动移动控制器返回锚点，战斗追击范围由警戒规则另行限制。
- 活动模式约束战斗追击边界：跟随模式围绕主人作战并沿用跟随/超距传送规则；等待模式主动扫描锚点约 6 格内的目标，响应与追击边界为锚点约 8 格，切换或重复选择等待模式时立即清除旧目标并重设锚点；闲逛模式在 16 格活动区内发现目标，最多追至距锚点约 24 格后放弃并返回。和平警戒下不会因发现目标主动追击。上述距离为初始值，可依据寻路与战斗测试微调。
- 索敌、追击和实际近战命中距离是彼此独立的规则。主动出击只能主动取得当前拥有直接视线的敌人，不能隔墙、隔地或穿过封闭方块发现目标；已经取得的目标失去视线后最多保留 40 tick 重定位时间，超时便清除并停止向不可见位置追击。英灵或主人刚被敌人直接伤害时，伤害来源可作为一次短暂暴露，即使当刻被遮挡也只保留同样的 40 tick。已经提交的完整攻击或技能不会仅因短暂失去视线而被强行中断，但实际命中仍必须遵守各技能自己的距离、角度与视线规则。军团兵普通挥刀只有在伤害判定帧仍满足原版实体近战范围且与目标之间有视线时才会造成伤害；目标在攻击起手后离开范围或躲到障碍物后方时不得隔空受伤。
- 警戒状态分为三种：主动出击会主动攻击允许范围内的敌对生物，并响应主人正在攻击的目标；被动防御只攻击伤害主人、自身或被主人攻击的目标；和平模式不主动攻击任何目标，但英灵自身受到直接攻击后仍可自卫。新录入遗物默认使用“跟随”活动模式与“被动防御”警戒状态。
- 所有英灵的常规主动索敌默认排除苦力怕。主人实际攻击某一只苦力怕后，该目标可按当前警戒、活动边界与 40 tick 视线记忆规则被英灵锁定并追击；仍然存活的苦力怕若实际伤害英灵或主人，也可触发对应的正常自卫。和平警戒仍只允许英灵因自身受击而自卫。军团兵的举盾冲锋可以对已蓄爆苦力怕造成 200% 攻击伤害、推离并短暂清空仇恨，但该反应式撞击本身不授予持续追击权限；范围、横扫或爆炸技能对苦力怕造成的自然误伤也不额外授权追击。
- 英灵不使用与玩家相同的盔甲栏和可替换武器栏；GUI 不为普通英灵装备预留槽位，构筑与强化统一由可放入、可取出的召唤器饰品承担。
- 召唤器提供六个完全开放的饰品槽，饰品可自由放入和取出。六槽的具体排列不锁定为 2×3，可在实际 GUI 排版和开发测试中调整而不改变玩法规则。
- 同名饰品最多安装一个，不同饰品可以同时安装并叠加。服务端槽位规则拒绝重复安装；召唤器属性区按实际最终数值显示，增加项为绿色、降低项为红色，中性数值保持原色。
- 基础英灵不自带背包，当前 GUI 不预留常驻英灵物品栏。未来可由占用饰品槽的“携行”类饰品提供少量储物格；届时必须单独定义英灵死亡、收回、遗物取出、召唤器转交及饰品拆除时的物品去向和所有权规则。
- 英灵共用同一套有限成长系统，等级上限为 30 级；GUI 显示当前等级和经验进度。等级与经验绑定并保存在英灵遗物上，遗物被取出或换入其他召唤器时完整保留成长；召唤器负责燃料和饰品。所有英灵的最大生命与攻击力从 1 级基础值线性成长至 30 级的两倍：罗马军团兵为 `30 → 60` 生命、`6 → 12` 攻击；阿兹特克勇士为 `34 → 68` 生命、`8 → 16` 攻击；埃及弓箭手为 `28 → 56` 生命、`5 → 10` 远程攻击；中国关刀战士为 `30 → 60` 生命、`7 → 14` 攻击；日本武士为 `28 → 56` 生命、`6 → 12` 攻击。护甲、移速、击退抗性和攻击频率暂不随等级提升。升至下一级所需成长经验暂按 `15 + 2 × 当前等级` 计算，从 1 级升至 30 级累计需要 1305 点成长经验。
- 活动模式、警戒状态和可选技能启用/禁用状态跟随英灵遗物保存；遗物换入其他召唤器后保留这些设置。新遗物默认使用“跟随＋被动防御”，普通开关技能默认开启；埃及弓箭手的箭型槽例外，默认关闭，并持久保存“关闭／叶形箭／锥锋箭”选择。日本武士与中国关刀战士的四个技能均可独立关闭。完整动作在起手时快照相关技能状态，已提交的普通攻击、踏込、刺或关刀连招允许自然完成，开关只阻止下一次起手；但残心临时闪避加成、越战越勇层数与减伤、甲胄傍身的投射物保护等持续收益在对应技能关闭时立即清除或停止生效，充能与冷却不因开关重置。等待和闲逛模式的具体位置锚点只属于当前召出的实体，不随遗物跨召唤保存；燃料与饰品仍属于召唤器。
- 英灵完成致命击杀时，目标按原版经验价值在世界中生成正常经验球，任何玩家都可以拾取；同一基础数值还会作为该英灵遗物的独立成长经验，不给予主人，也不要求拾取。直接攻击、英灵投射物、英灵延迟伤害及由英灵伤害引发的 10 秒内环境收尾可以归属该英灵；仅由主人造成的伤害不归属。基础经验大于 0 时，倍率结果四舍五入且最低为 1。`鹰纹宝石` 只令世界经验球增加 50%，`墨西加鹰首唇饰` 只令英灵成长经验增加 50%。
- 当前开发版已实现上述经验闭环：参与窗口为 10 秒，同一主人有多个符合条件的英灵时平均分配目标的原版经验值；等级和当前级经验保存在遗物上，升级会立即刷新在场英灵的生命与攻击属性，并以行动栏文字和玩家升级提示音反馈。满级后不再累计溢出经验。
- 士兵阵列是可在 GUI 技能栏手动启用或禁用的常驻光环，不要求附近存在敌人。实际增益通过无限持续时间的自定义状态效果实现：默认显示“举起武器！”并固定提供 +2 攻击；主人任一只手持盾时立即切换为“举起盾牌！”，继续提供同一份 +2 攻击，并额外驱动主人和同主人军团兵的 15% 最终伤害减免。两种状态各自使用独立的 18×18 图标。范围采用“8 格进入、超过 8.75 格连续 10 tick 才退出”的滞回；技能关闭、来源卸载/消失、主人离线/死亡/旁观、跨维度或绑定失效时不享受退出宽限并立即撤销对应主人收益。服务端每 10 tick 反向审计无限效果和永久属性修饰器：无合法来源时强制清理，存在来源但修饰器丢失时重建；多个来源只取最强盾阵列且切换时原子替换，避免永久残留、叠层和短持续时间闪烁。GUI 中士兵阵列技能始终使用普通白边技能框，不以黄色外框表示生效。
- 消耗灵魂介质产生召唤燃料；每次召唤支付一定燃料，英灵自然恢复生命时持续消耗少量燃料。属性中的“召唤消耗比例”同时修正一次召唤成本和自然生命恢复的燃料成本，默认值为 100%；所有天赋、饰品等修正后的最终比例显示在 GUI 中。
- 英灵存活期间没有持续维持燃料消耗。燃料耗尽不会使在场英灵消散，也不妨碍其正常战斗，但会停止需要燃料的自然生命恢复；燃料只在实际召唤和实际恢复生命时扣除。
- 英灵仅在脱离战斗约 5 秒后开始自然恢复生命；受到攻击、重新锁定攻击目标或再次参与战斗时立即停止恢复。恢复按实际补回的生命值和当前召唤消耗比例扣除燃料，满血或未实际恢复时不扣燃料，燃料不足则暂停恢复。比例修正产生的小数燃料成本使用内部定点累计，累计到可扣除整数时再实际扣除，不在每次恢复时粗暴取整。具体恢复间隔和每点生命的基础燃料成本继续通过数值设计与实测确定。
- GUI 提供一个燃料输入槽。有效燃料放入后自动转化为召唤器内部燃料；达到容量上限时停止转化，未消耗的剩余物品保留在槽内并可取回，无效物品不能放入。当前数值为：内部燃料容量 1000，灵魂沙与灵魂土均提供 50，腐肉提供 20，每次召唤消耗 100，自然恢复每 2 秒恢复 1 点生命且每点消耗 2 燃料。燃料条悬浮时显示当前值、最大值和单次召唤所需燃料；若剩余容量不足以容纳一个燃料物品的完整价值，则暂不消耗该物品，避免燃料溢出浪费。上述数值允许依据实测节奏调整。
- 燃料槽在召唤器位于玩家物品栏且 GUI 关闭时仍每 5 tick 最多转化 1 个有效燃料。仅同一个召唤器实例的内部容器数据或燃料值变化不得触发第一/第三人称换手下沉动画；玩家真正切换到另一件物品或另一只召唤器时仍保留原版换手动画，主手与副手规则一致。
- 灵魂沙与灵魂土效率较高且完全等价，腐肉效率较低。
- 英灵召唤后不再因持续时间、主人离线或主人死亡自然消散；只在玩家主动收回/遣散、英灵死亡、遗物被取出/替换、召唤器被确认销毁或绑定被管理员终止等明确条件下离场。召唤器作为掉落物、放入普通容器或转交他人都不会遣散英灵；直接销毁召唤器或销毁装有召唤器的原版嵌套容器物品会同时终止绑定，未知模组容器不能确认内容时优先避免误杀。
- 只有“跟随”状态会随控制者跨维度迁移；等待与闲逛留在原维度和原锚点继续运行，区块卸载后按原版实体规则暂停且不会强制加载区块。控制者离线、死亡或处于旁观模式时，跟随英灵留在当前位置自卫并停止主人定向技能；等待/闲逛照常行动，自身技能照常工作。控制者重新可用后，跟随绑定可在其附近安全重建，旧实体和旧投射物通过绑定代数失效，不会复制或追到错误维度。
- 召唤器持有者可以修改燃料、饰品、遗物、活动模式、警戒状态和全部技能开关；这些操作本身不改变英灵控制权。只有活跃英灵被第三者明确切换到“跟随”，或英灵已经离场后由第三者重新召唤，才把控制权交给该玩家。跟随接管会取消旧实体仍未结算的动作和投射物，但已经造成的伤害、已落地的有限效果、遗物充能与按服务端游戏时间记录的技能冷却保留。
- 绑定 SavedData 是召唤器内容、燃料、遗物成长/经验/技能状态、饰品、控制者、实体代数和存活状态的服务端唯一权威；物品 ItemStack 只作为镜像。英灵击杀参与经验直接写入权威遗物，即使召唤器在箱子、掉落物或其他未加载位置也能实时结算；物品下一次进入玩家可见库存时同步。并发菜单以状态修订号拒绝过期操作；检测到复制出的相同召唤器 UUID 时，先被登记的原件保留绑定，后出现副本获得新 UUID、保留物品内容但不继承控制权。
- 英灵死亡或主动遣散后不设置额外复活冷却；再次召唤正常支付燃料并以满血生成。遗物上的技能冷却与充能继续按服务端游戏时间推进，死亡和离场不会暂停或重置；服务器关闭期间游戏时间不流逝。跨维度重建保留当前生命、吸收、燃烧/冰冻/空气与已经获得的有限自强化，但取消未完成动作、无重力、临时碰撞/护甲窗口和旧代投射物。
- 默认不限制单个控制者拥有的存活英灵数量；`config/echo_warrior-bindings.json` 的 `maxLivingEchoesPerController` 可设置硬上限，`0` 表示无限。成功召唤使个人存活英灵超过 8 名时提示服务器性能风险，每名玩家按现实时间一小时持久冷却，只有新的成功召唤触发提示。
- 当前未发布开发版把英灵绑定格式定为 schema v1，不提供旧开发世界迁移；采用本设计后测试世界应重新生成。管理员可用 `/echo_warrior binding list|status|dismiss|repair` 诊断、终止或保守重建活跃跟随绑定，未来已发布版本再为 schema 变化提供兼容策略。

当前召唤器开发实现状态（2026-08-31）：

- 已接入遗物 0～4 个随机天赋、互斥规则、一次性初始化、复制持久化和开发用重掷命令。
- 已接入 1000 容量燃料、腐肉/灵魂沙/灵魂土转换、安全召唤、自然恢复消耗及 GUI 实时显示与转换粒子。
- 已接入跟随/等待/闲逛与主动/被动/和平模式的遗物持久化和基础活动边界。
- 军团兵四项技能已有可运行首版，固定顺序为士兵阵列、军团壁垒、举盾冲锋、军团永存。军团壁垒是可独立开关的常驻被动，按受击瞬间的身体朝向判定前方 180°：来自该区域的直接攻击与投射物伤害在护甲结算后降低 50%，背后攻击、爆炸、环境伤害和持续状态伤害不受影响；成功保护实际生命损失时播放轻量盾击声与格挡粒子。举盾姿态由独立的服务端同步状态和 GeckoLib 姿态控制器维持，举盾冲锋结束或军团永存完成 5 秒蓄力并释放后才播放收盾。举盾期间渲染层完整抵消上半身举盾姿态传递给头部的父骨骼旋转，使头部保持笔直并朝向实体正前方；瞳孔仍可独立追踪周围目标。举盾冲锋使用正弦速度曲线形成“平稳起步—中段加速—末段收势”，并改用前后 tick 与下一 tick 预测段的连续相对轨迹检测高速投射物；投射物分支不再等待 6 tick 才能结算，反弹时保留原速度与伤害、将归属改为军团兵，并依次瞄准仍合法的原射手、16 格内最近可见敌对生物，均不存在时沿远离主人的方向弹飞。潜影弹等追踪型投射物同步替换或清除追踪目标，不会转弯继续追主人。命中蓄爆苦力怕时造成当前攻击力 200% 的普通伤害，保留原有“远离主人 70% + 冲锋方向 30%”强击退，并在 40 tick 内持续清空目标、压低自然蓄爆方向；被打火石强制点燃的苦力怕不会因此清除强制点燃标记，仍可能在被撞远后爆炸。冲锋沿途继续播放低频风弹掠过声和少量尾流，两类命中统一播放盾牌撞击声。命中或取消后水平速度立即衰减至 15%，只保留短暂惯性；0.5 秒内置冷却从冲锋结束时重新计算，已命中的同一目标另有 2 秒重复拦截保护。军团永存蓄力期间在自身头顶零星生成沿抛物线先上升后回落的生气粒子，以此取代触发时脚下的风爆反馈；释放声暂用风弹爆发音效。具体范围、路径、击退、粒子和声音仍需实机手感测试后调参。
- 埃及弓箭手已接入可编译的完整 MVP 战斗逻辑：24 格射程、按“取箭搭箭 → 拉弓 → 满弓稳定瞄准 → 松弦放箭”拆分的 42 tick 基础射击状态机、目标无关的连续取箭与无目标收箭归袋、目标死亡后的无跳帧恢复、与身体朝向解耦的世界坐标撤退及受阻寻路、边近战边撤退的紧急求生、两种持久箭型、齐射、完整碰撞检测的全向安全后撤步、亡灵增伤、猫神苦力怕保护、末影龙部件瞄准、专用箭矢及粒子尾迹，并已复用三英灵统一的眼神与神情系统。五个技能槽现均使用用户人工绘制的正式 16×16 图标；仍需实机验证连续取箭/收箭、三维稳定瞄准、近战撤退、复杂地形寻路、末影龙战斗、按钮局部粒子与多人同步。
- 中国关刀战士已接入首版完整战斗闭环：12 护甲、投射物伤害与击退减免、随有效挥击叠加的攻击强化和刀刃火焰、按缺失生命线性提高的最终减伤、220° 普攻范围斩击，以及按模型关键帧运行的四段智能连招。实体模型、遗物图标和四个用户人工绘制技能图标均已规范化接入；连招第一击前会围绕锁定主目标进行最多 0.75 格的安全追踪修正，近距离受到合法近战攻击时会立即转身反击。攻击与连招使用持有末帧、稳定待机底层和零 tick 动作层释放；边界诊断日志经实机确认收尾稳定后已恢复为默认关闭，可按命令重新开启。完整视觉生命感系统也已适配；仍需实机验证命中扇区、追踪修正、四段前进距离、一格台阶升降、第一段浮空、第二段投射物弹开、动作收尾与多人同步手感。
- 六个饰品槽已接入服务端兼容性框架与收纳袋式直接放入路径：只有实现饰品接口或属于饰品物品标签的物品可进入首个兼容空槽。同名饰品最多安装一个，不同饰品可以组合；插入或取出后，在场英灵立即更新，玩家本身不受影响。攻击、护甲和最大生命均为等级成长完成后的固定加减值，最终攻击和护甲不低于 0；陆地移速修正相对基础值相加，最终不低于基础值的 25%，水中加速独立计算。最大生命改变时保留当前生命的绝对数值，只有超过新上限时才截断。
- 当前 25 件正式饰品为：罗马环片坠、锁帷重带、黑曜荆棘符、百战砺符、负山根付、裂曜晶坠、双鱼玉徽、盲剑目隐、裂响祭祀石、凯旋桂冠、血契河马牙、阿努比斯护符、身代木偶、玉蝉、盛宴胸针、玉璧、托纳蒂乌花冠、孔苏月露瓶、尼罗河红鱼符、猫纹玉、鹰纹宝石、墨西加鹰首唇饰、鹰纹远目镜、风神羽饰与尼罗鹮骨。精确数值、触发边界、配方和绘制说明以 `encyclopedia/content/atlas.zh_cn.json` 对应独立条目为准。
- 25 件饰品统一采用简洁的两层物品提示：默认逐行显示效果，正面效果以 `+` 和知识碎片同款绿色 `#93CE85` 标示，负面效果以 `-` 和柔和红色标示；复杂的攻击窗口、维度判断与触发边界不挤入基础提示，仍由百科保存精确定义。效果下方以深灰色显示“按 Shift 查看更多”；按住 Shift 后改为“放入召唤器，为英灵提供装备效果”和“同名饰品不可重复佩戴”，这两条说明及其开头的 `+` 均使用灰色，只有“召唤器／英灵／饰品”使用淡纸色 `#FFEDC9`。饰品名称继续使用原版稀有度颜色：常见白色、精良黄色、稀有青色、史诗紫色；玉璧为史诗（等级 3）。Shift 状态由客户端供应器共享给通用提示代码，专用服务器默认按未按下处理。
- 关键战斗规则已经固定：黑曜荆棘符为 `+1` 护甲并按实际近战生命损失 `1:1` 非递归反伤；身代木偶在武士残心判定之后独立进行 10% 合格攻击闪避；裂响祭祀石每个攻击窗口独立进行 30% 的 200% 暴击；血契河马牙每个攻击窗口 30% 恢复 4 点生命；凯旋桂冠在任意有效生物击杀时恢复 10% 饰品修正后最大生命；阿努比斯护符提供 `+2` 攻击并在击杀时以 0.5% 概率凝炼一个随机传承。
- 主动索敌视野修正只改变带视线检查的主动目标获取，不改变渲染距离、攻击距离或既有目标的追踪：鹰纹远目镜为基础值的 150%，上限 32 格；盲剑目隐为基础值的 25%；等待模式仍受约 8 格上限。猫纹玉持续压制 6 格内苦力怕的引信与爆炸，并且只在英灵处于“主动出击”警戒时允许主动锁定苦力怕；其他警戒模式仍遵守原有受击与主人攻击响应。
- 托纳蒂乌花冠与孔苏月露瓶只在具有天空光照的昼夜维度按原版时间分别提供生命恢复 I，不受天气或能否看见天空影响；两者可以同时安装。尼罗河红鱼符为红色鱼玩偶，而不是靴子，提供 50% 水中移动加速。风神羽饰与尼罗鹮骨均采用独立直接配方；此前设想的饰品升级链已经取消，当前没有饰品以另一件饰品为合成材料。
- 五种传承已经采用正式名称与主动逻辑：勇气、坚韧、纯净、智慧均右键阅读 16 tick，分别给予 60 秒力量 II、60 秒生命恢复 II、清除全部负面状态、给予 100 点原始经验；工艺的传承没有右键效果，与恰好一件已损伤工具或护甲无序合成时恢复向上取整的 20% 最大耐久，并保留附魔、名称与其他组件。五种物品名依次使用 `#B33A24`、`#4F9E2F`、`#E3E3E3`、`#2A86C2`、`#E0B72D`，除纯净外均提高饱和度。悬浮提示默认显示灰色 `+` 开头的用途与英灵饰品合成材料说明，正向用途关键词使用饰品同款绿色 `#93CE85`，“负面效果”使用柔和红色，“英灵饰品”使用淡纸色 `#FFEDC9`，随后以深灰色显示“按 Shift 查看更多”；按住 Shift 后改为以深灰斜体显示“一册来自古战场的典籍...”与“在不同的时代，我们总能够找到相同的、值得传承的高尚品质...”两行风味文字。所有传承堆叠上限为 64，非创造模式成功使用后消耗 1 个。
- 25 件饰品继续使用稳定资源路径 `echo_warrior:item/<饰品 ID>`。模型师制作的全部 16×16 RGBA 正式图标已于 2026 年 8 月 30 日接入游戏与百科；注册 ID、功能、文明标签和掉落映射不因展示名称与美术调整而改变，原始 Aseprite 与逐件 PNG 保留在 `human-work/英灵饰品/`。
- 创造模式中“英灵回声”主标签页固定排在本模组标签页最前；独立的“英灵饰品”标签页以阿努比斯护符为标签图标，按百科目录顺序集中展示全部 25 件饰品，这些饰品不再重复出现在主标签页。
- 25 件饰品的最终合成表已于 2026 年 8 月 30 日完成人工审核并同步至游戏与百科。配方中的“羊毛、木板、树叶、鱼”分别接受任意原版羊毛、任意原版木板、任意原版树叶和任意原版鱼类；其余材料均指表中明确的单一原版物品。
- 手工测试项目记录于 `docs/SUMMONER_TEST_CHECKLIST.md`；可交互版本为 `docs/SUMMONER_TEST_CHECKLIST.html`。

后期候选升级：

- 玩家受到攻击时自动召唤。
- 阻挡致命伤害并召唤英灵，带冷却时间。
- 玩家受到的部分伤害由英灵承担。

### 2.4 英灵

比赛 MVP 的最低验收仍为前三个差异明显的英灵；在前三英灵开发闭环已经建立后，用户于 2026-08-25 批准中国关刀战士作为第四位正式开发英灵，并于 2026-08-26 批准日本武士作为第五位正式开发英灵。额外英灵不改变考古、回声罗盘和基础升级仍需完成的 MVP 主线。

- 第一名：罗马军团兵——持盾近战、防护和吸引敌人。
- 第二名：阿兹特克勇士——沉重横扫、致死流血、日照强化和跳劈追猎。
- 第三名：埃及弓箭手——移动射击、箭型切换、齐射与全向后撤。
- 第四名：中国关刀战士——高护甲、范围斩击、越战越勇与四段推进连招。
- 第五名：日本武士——中甲、高机动、依赖闪避循环的单体决斗者。

#### 2.4.0 罗马军团兵平衡修订（2026-08-31）

- 1 级基础属性继续保持 30 生命、8 护甲、6 攻击、0.28 移速、30% 击退抗性和 20 tick 基础攻击间隔。横向比较后确认其裸属性并非五名英灵最低，短板主要来自单体条件连击和缺少独立常驻生存机制，因此本轮不直接抬高生命、护甲或攻击面板。
- 新增可独立开关的被动“军团壁垒”，并将其安排为第二技能：以军团兵受击瞬间的身体朝向为中心，前方 180° 内来源的直接攻击与投射物伤害在护甲结算后降低 50%。背后攻击、爆炸、摔落、火焰等环境伤害，以及中毒、流血等持续状态伤害不触发；箭矢已进入碰撞位置导致来源方向不稳定时，使用投射物来向回退判定。成功减少实际生命损失时提供轻量格挡粒子和盾击声。
- 条件二连击的第二击由 50% 提高至 75%，首击、追击范围、命中时机、追步距离、击退与动作后冷却均不变。该调整只提高成功维持同一合法目标后的回报，不把军团兵扩张为关刀或马夸威特式范围输出者。
- 举盾冲锋的反制能力同步增强：高速投射物使用连续相对轨迹检测并允许即时格挡，反弹优先返回原射手、其次瞄准 16 格内最近可见敌对生物，否则远离主人弹飞；伤害与速度不额外放大。撞击蓄爆苦力怕造成 200% 当前攻击伤害，并令其 40 tick 内持续失去目标；既有击退方向、3 层充能、每 5 秒恢复 1 层和反应式触发条件不变。
- 士兵阵列的 +2 攻击与持盾时 15% 最终减伤、举盾冲锋的 3 层充能与每 5 秒恢复 1 层、军团永存的 5 秒防御、+12 护甲和 20 秒冷却本轮保持不变。军团壁垒已经使正面有效生存翻倍，先通过实机验证其与士兵阵列、军团永存的叠加边界，再决定是否继续上调其他技能。
- 技能布局版本升至 v2。旧三技能罗马遗物会先补上默认开启的军团壁垒，再与已有四技能遗物一并按“士兵阵列、军团壁垒、举盾冲锋、军团永存”重排开关位；玩家原先对各技能的启用状态不会串位，也不要求重做遗物。军团壁垒现使用 `human-work/罗马军团战士军团壁垒.png` 提供的独立 16×16 人工图标，并同步到游戏资源与网页百科。

#### 2.4.1 阿兹特克勇士已冻结规格（2026-08-19）

- 实体标识为 `aztec_warrior_echo`，中文名为“阿兹特克勇士英灵”，英文名为 `Aztec Warrior Echo`。
- 1 级基础属性：34 生命、4 护甲、8 攻击、0.30 移速、20% 击退抗性；基础攻击间隔为 29 tick（约 1.45 秒）。
- 五项技能均可在召唤器 GUI 中独立开关：羽蛇神的诅咒、维齐洛波奇特利的祝福、黑曜石创口、追猎、马夸威特。关闭技能不清空已存充能，已施加的短时状态按自然持续时间结束。
- 羽蛇神的诅咒只在阿兹特克勇士或主人实际承受可归因的直接伤害时检定，触发率 60%；触发后 75% 施加虚弱 I 5 秒，25% 施加虚弱 II 3 秒。虚弱 I 不会降级已存虚弱 II。
- 维齐洛波奇特利的祝福以“谁在日光下，谁享受”为原则独立检定英灵与主人。白天且直视天空时获得 +15% 近战攻击与每 4 秒 1 点的缓慢恢复，雨雪与雷暴不取消，不消耗召唤器燃料。满足条件时显示为无限持续时间的独立状态效果，离开日照后立即移除，以避免短时效果反复刷新造成图标闪烁；英灵与玩家均关闭该效果的原版药水粒子，玩家仍可在状态栏查看图标。祝福还会在技能关闭、遗物更换、英灵死亡/遣散、绑定失效、玩家进出服务器或服务端发现不存在有效来源时立即清理，不能残留为永久玩家效果。阿兹特克勇士处于丛林、沼泽、红树林沼泽、森林或林地时额外获得 +10% 攻击与 +10% 移速。
- 黑曜石创口持续 6 秒，每秒造成 1 点可致死伤害，不叠加但可刷新。普通攻击主目标触发率 30%，马夸威特溅射与追猎落地命中的每个目标各自独立以 15% 触发。伤害类型绕过受击冷却，不与普通近战伤害争抢无敌帧。
- 马夸威特使普通攻击在前方 160°、2.5 格内横扫，主目标承受 100% 伤害，溅射目标承受 50% 伤害，并必须通过视线与友军检查。
- 追猎为 AI 自动施放的两层充能技能，每层 6 秒顺序恢复，两次施放间隔至少 2 秒；只对水平 4～10 格、高度差 +4～-6 格的已锁定目标尝试，确认安全落点与无障碍跳跃路径后才消耗充能。追猎开始至落地前的 18 tick 免疫普通伤害，但带有 `BYPASSES_INVULNERABILITY` 标签的绝对伤害仍可生效；落地时对 2 格内敌人造成当前 100% 攻击伤害与轻微击退。完整 24 tick 追猎动作期间，受伤不会切换为全身受伤动画。
- 追猎落地后获得 3 秒强势期：+25% 移速、20% 伤害减免、+50 个百分点击退抗性；马夸威特横扫扩大为 180°、3 格、75% 溅射，攻击间隔不变。
- 普通攻击一旦开始，其完整 29 tick 动作不会被全身受伤动画替换；英灵仍会正常承受伤害、减益与击退，并继续播放代码驱动的疼痛眨眼和伤害来源注视，因此实际位移仍可能令挥击落空。
- 阿兹特克勇士复用罗马军团兵的视觉生命感逻辑与全部已确认参数，包括玩家注视与互相对视、被主人发现偷看后的反应与离场、眨眼、歪头、瞳孔追踪、苦力怕警觉和受击来源注视。普通行走时会优先看向前进方向，头部与瞳孔只保留小幅侧视，不再持续回头盯着主人；战斗、攻击、追猎与紧急威胁始终优先于该限制。运行时骨骼映射按阿兹特克模型的命名与父级结构单独适配。
- 模型源文件保存于 `assets-source/blockbench/aztec_warrior_echo.bbmodel`。运行时资源由 `scripts/update_aztec_visual_assets.py` 生成；脚本会把原 `animation`/`animation2`/`jump attack` 等名称规范化，并展开 `jump attack` 中 `Arm_Right` 跨越 ±180° 的欧拉角，避免预览插值导致武器乱飞。
- 阿兹特克勇士的五个 16×16 技能图标、黑曜石创口与维齐洛波奇特利祝福的 18×18 状态图标均已替换为人工绘制资源。规范化后的运行时文件名与美术源文件映射记录在 `docs/AZTEC_ART_TODO.html`。
- 模型的规范源贴图保存为 `assets-source/textures/entity/aztec_warrior_echo.png`。资源同步脚本会先将该 128×128 PNG 写回 BBModel 的内嵌纹理，再导出运行时贴图，避免后续动画重导出恢复旧皮肤。

#### 2.4.2 埃及弓箭手已冻结规格（2026-08-21）

- 实体标识为 `egyptian_archer_echo`，遗物标识为 `egyptian_archer_relic`。1 级基础属性为 28 生命、0 护甲、5 远程攻击、0.30 移速、15% 击退抗性；满级生命与攻击分别为 56 与 10。基础警戒与射击距离为 24 格。
- 每次远程攻击按语义拆成 `NOCK（取箭搭箭） → DRAW（拉弓） → AIM（满弓稳定瞄准） → SHOOT（松弦放箭）`。基础 42 tick 的预算为 `23 + 8 + 6 + 5`，当前“苗条”攻速的 33 tick 预算为 `18 + 6 + 5 + 4`，允许的最快 24 tick 预算为 `13 + 5 + 3 + 3`；24～42 tick 之间按同一权重分配且每个阶段保持最小时长，埃及弓箭手的实际攻击间隔不得低于 24 tick。攻击间隔只表示理想的放箭间距，转身、视线和重新瞄准可以延长本轮。`AIM` 只有在目标存活、24 格内可见且身体/头部的三维注视方向稳定对准实际瞄准点时才倒计时；同一目标短暂偏离或遮挡只暂停，严格更高威胁换成新目标时重新计算完整 `AIM`。`AIM` 期间双臂、弓弦和手持箭固定在模型师满弓末帧，身体、头与瞳孔仍逐 tick 跟踪目标。首次 `NOCK` 使用 `DRAWBOW` 的 `0 → 1.70833 秒`，首次 `DRAW` 使用 `1.70833 → 2.25 秒`；连续射击使用从射后举弓姿势派生的对应取箭与拉弓片段。`SHOOT` 删除原始 `0 → 0.08333 秒` 的重复满弓停顿，仅播放 `0.08333 → 0.33333 秒` 的真实松弦；服务端分别在 5/4/3 tick 释放阶段的第 3/2/2 tick 生成箭，保证不早于模型箭矢在新片段约 50% 处消失。攻速只改变各阶段播放倍率，不改变动作语义或关键帧顺序。
- 连续射击的 `NOCK` 不依赖当前目标：上一支飞行箭或其他伤害在取箭途中击杀目标时，不停止、不重播也不闪回第一帧，选择器可在这期间转向严格更高威胁；只有 `NOCK` 完成时才要求存在可见且在射程内的有效目标以进入 `DRAW`。`SHOOT` 完成时若仍有有效威胁，立即从射后举弓姿势开始下一次连续 `NOCK`，不再等待上一支主箭的 4 tick 飞行判定窗口；若当时完全没有目标，则直接播放 10 tick 收弓。若连续 `NOCK` 已经开始后最后一个目标才死亡，先自然完成本次取箭，再用固定 12 tick 的反向取箭片段把箭放回箭袋，随后用原有 10 tick 收弓；收箭途中重新出现目标也会先完成收箭与收弓，避免动作反转抽搐。手持箭仍以 48 Hz 按右手完整父级变换重新烘焙，但显隐缩放强制使用线性插值，禁止原 Catmull-Rom `0 → 1 → 1` 产生超过 1 的尺寸过冲；所有切片共享精确采样边界并由生成脚本验证零姿势漂移。
- 普通转火可在 `NOCK`、`DRAW` 与 `AIM` 期间发生，同威胁等级目标保持粘滞；新目标在 `AIM` 中必须重新完成稳定瞄准。进入 `SHOOT` 后冻结本次目标：普通转火或新遮挡不取消箭，目标仍存活时在约 32 格提交容差内照常生成投射物并由实际方块碰撞处理掩体；目标死亡或移除则完成松弦姿态但不生成空箭。只有真正进入即时近战范围的敌人可以在箭生成前打断 `SHOOT`，箭生成后则允许立即切入不可打断的备用近战。移动射击只控制上半身，下半身继续按实际速度播放行走；站立射击恢复站姿。攻击、射箭、近战与后撤步期间，全身 `hurt` 动画不得打断当前动作。
- “赞美猫神”为 30 格球形光环。苦力怕正常进入范围不会立即逃跑；只在尝试点燃或受到具有直接实体来源的伤害时取消点燃并进入约 5 秒慌乱逃窜。技能关闭后保护立即失效，已开始的慌乱按剩余时间结束。该技能还拥有只在玩家百科公开的隐藏联动：技能开启且警戒状态为“主动出击”时，埃及弓箭手可主动取得可见苦力怕，并在猫神效果令其慌乱后继续射击，直到目标按通常的死亡、失效、活动边界或视线记忆规则被释放；关闭技能或切换至其他警戒状态后恢复共用的苦力怕索敌规则。召唤器技能悬浮说明不写入此联动。
- 箭型技能占用一个循环按钮，按“关闭 → 叶形箭 → 锥锋箭 → 关闭”切换，服务端切换冷却为 10 tick。选择跟随遗物持久保存，新遗物默认关闭。叶形箭在正常箭伤害后施加 4 秒共 2 点、不叠加但可刷新的流血，以及 2 秒轻微减速，尾迹为黄色；锥锋箭在伤害前忽略目标 35% 护甲，并以 25% 概率贯穿当前目标、最多继续命中 1 个额外目标，尾迹为黑色；普通箭无特殊尾迹。专用箭矢不可拾取，落地后短时消散。
- 所有直接攻击，包括普通箭、两种特殊箭、后撤步齐射与备用近战，对亡灵额外造成 20% 伤害；叶形箭的后续流血不享受亡灵增伤。箭矢会穿过主人、同主人英灵、主人的驯服生物、同队实体及创造/旁观玩家，不造成友军伤害。
- “战车与齐射之魂”开启时允许移动射击，但战斗中只进行必要位移：任一有效近身威胁进入 8 格时进入风筝撤退，直到最近威胁退出 10 格才解除。开放地面每 tick 检查短距离碰撞、落脚面和危险方块后，以不依赖实体朝向的世界坐标水平速度持续沿平滑后的远离向量移动，身体与头部仍面向当前主要目标；只有直后方不安全或确实受阻时才依次尝试斜后方、侧向的稳定寻路，且不会按固定周期反复换路。遇到一格高障碍时，以同一套完整碰撞箱探针确认上方和落脚面安全后立即给予普通跳跃冲量，并在腾空跨越期间持续保留向后的水平撤退速度；该跳跃不消耗后撤步充能也不触发齐射，二格墙和危险落点仍会阻挡。10～20 格且视线通畅时同时停止导航、残留输入与本系统拥有的撤退速度，以站立射击为主；超过 20 格或失去视线时才接近或重新寻找射击角度，非战斗状态的跟随、走路与闲逛不受影响。持续失去视线时最多只重新寻路 20 tick，并优先切换到可见敌人；超时后放弃目标，重新看见时才允许获取，不能隔墙或隔地无限追踪。关闭被动后同时禁用上述常规距离控制与追加齐射，但不会禁用贴身自救撤退。16 格内至少存在两名有视线的有效敌人时，每次主射击追加一箭的概率为 `15% + 8% × 额外敌人数`，最高 55%，追加目标从主目标之外随机选择且每次最多一个；追加箭成功生成时，英灵身体周围爆发一次 14 颗金黄色尘埃粒子。战斗中有敌人进入 8 格时强制进行近身威胁评估，优先级依次为最近实际伤害来源或已进入实际近战范围、正在攻击英灵或主人的敌人、近期伤害主人的敌人、其他敌人；同级目标一旦锁定就保持粘滞，不按细小距离变化来回切换，只有严格更高优先级威胁能覆盖。高威胁锁定在目标退至 10 格外、死亡或失效后解除。英灵自身受到可归因伤害时，若当前目标位于 8 格外，则 24 格内的伤害来源获得 60 tick 紧急优先级；新的伤害来源刷新并替换该目标，8 格内近身威胁仍可覆盖。普通转火沿用正在进行的 `NOCK`/`DRAW` 相位，新目标在 `AIM` 中重新完成稳定计时；但任何敌人进入约 2.6 格的实际近战范围后，会在目标失效恢复、取箭、拉弓、瞄准或箭尚未生成的松弦阶段之前立即抢占为备用近战。贴身判断按双方碰撞箱的净间距计算，不再让蜘蛛等宽体型敌人因为中心点较远而漏判。若敌人已经用直接近战成功命中弓箭手，则同步触发该抢占，不再等待下一次索敌或射击间隔；这条自卫路径不受等待/闲逛活动锚点的主动追击边界限制。近身自卫目标会独立保留到死亡、失效或离开英灵约 10 格：近战、后撤步和落地后的重新瞄准沿用同一目标，即使双方处于等待/闲逛锚点外也不会把它误判为脱战；但该保留只允许反击、原地瞄准和远离威胁，不允许因失去视线而向锚点外追击。最近 1 秒内实际伤害过英灵的贴身敌人优先，其次为正在攻击英灵或主人的贴身敌人。近战起手会取消未完成的远程及受伤动作，但不会停止求生撤退：完整 20 tick 近战只控制上半身，下半身继续以基础移速 85% 沿远离 8 格内全部有效近身威胁的加权方向移动，最近实际伤害来源获得额外权重，开放地面优先直退，受阻才选择稳定斜后/侧向并允许普通跳上一格台阶。最近威胁达到 4 格后结束贴身撤退并恢复移动射击/常规风筝。本次近战动作不被索敌字段清空、受伤、射箭或后撤步覆盖；若目标在伤害帧前死亡或失效，只允许在不重启动画、不延长时长的前提下改打一次最近合法贴身敌人，伤害帧后不再转移。命中时造成 3 点伤害（亡灵 3.6）并按原版击退抗性施加 0.6 强度击退；已在合法贴身距离起手的挥击允许约 4 格的受击击退容差，但仍要求目标存活、可攻击且保持视线。
- “后撤步”在最近敌人进入 4 格时触发，拥有 2 层充能，每层 120 tick 顺序恢复，技能关闭期间仍在后台恢复。施放前在任意角度搜索约 5 格安全落点，按完整碰撞箱、20 tick 抛物线采样、地面与火焰/熔岩/细雪等危险检查选择最远离最近敌人的安全一侧；起跳后严格保留开始时的当前朝向，不读取寻路或目标更新造成的临时偏转。没有全程安全路径时不移动、不消耗充能，并允许使用 3 点、1 秒间隔的备用近战；若跳跃过程中环境变化使下一步不再安全，则停在最后安全位置并结束动作，碰撞箱不得穿入墙体。跳跃约 20 tick，完整播放模型师制作的 `backstep_shoot`；模型在第 13 个 24 FPS 关键帧（约 0.54167 秒）隐藏手持箭并释放弓弦，因此服务端在第 11 tick 重新扫描 12 格内至多 6 个仍存活且有视线的敌人并统一齐射。原目标死亡或躲入掩体时可自然改射当时的其他有效目标；若已经没有可见目标，则不生成幽灵箭但仍自然播完动作。后撤步可打断待机、行走、受伤与拉弓，但不会打断已经开始的备用近战或已进入放箭阶段的 `SHOOT`。近战结束时，若仍有敌人位于 4 格内、刚才再次受击、撤退受阻约 0.5 秒，或 6 格内至少有两个敌人/生命不高于 40%，则立即尝试消耗后撤步；威胁已经拉开到 4 格外时不浪费充能。
- 埃及弓箭手沿用罗马军团兵与阿兹特克勇士的完整视觉生命感系统，包括自然注意、互相对视、被主人发现偷看后的反应与离场、眨眼、歪头、瞳孔追踪、苦力怕警觉和受击来源注视。埃及模型使用 `Head`、`Eyes_Left`、`Eyes_Right` 与 `Eyebrow` 骨骼映射；一旦存在有效战斗目标，社交、环境观察、苦力怕警觉和受击来源都不能再覆盖注视点，当前射击目标（末影龙则为实际瞄准部件）逐 tick 硬接管身体正面、头部和双眼瞳孔，歪头归零并提高追踪响应。索敌字段在普通目标死亡或短暂失效的动作宽限内仍会优先沿用本次动作目标并保持最后同步战斗注视点；持续不可见目标的 40 tick 视线记忆超时后则立即释放锁定，不把头部继续固定在墙体或地面后的敌人上。`SHOOT` 已提交后即使出现新遮挡，也会在释放完成前继续注视已提交的实体或末影龙部件；近身自卫目标和后撤步提交目标同样使用战斗注视锁。最后一个目标失效并释放战斗锁后进入 30 tick 战后收势：若已开始 `NOCK`，会先完成取箭、12 tick 收箭归袋和 10 tick 收弓；收势期间保持最后身体正前方，若开始移动则连续接到行进方向。行进注视在短暂停步后另有 20 tick 释放滞后，状态稳定后才恢复一次较慢的自然注视。渲染层从取箭、拉弓、满弓瞄准、松弦一直到收箭/收弓完成，均按模型真实的 `Main` → `Upper_Body` → `Upper_Body2` → `Head` 父链组合旋转求精确逆变换，避免多轴姿态交接把正确的世界注视点带成抬头、侧看或单帧抽动；眨眼和受伤等表情细节仍可保留。服务端只在注视锁或目标发生切换时输出 `[EgyptianArcherGaze]`，近身自卫目标的获取与释放用 `[EgyptianArcherSelfDefense]` 记录，并在战后短窗口用 `[EgyptianArcherPostCombatGaze]` 记录实际注意目标交接；客户端 `[EgyptianArcherGazeClient]` 继续利用最终渲染后的 `Head`、`Eyes`、`Hat` 世界坐标还原脸部正前方，只在转火稳定后仍持续超过阈值的世界空间视线误差时限频输出。
- 2026-08-23 的后续日志确认，剩余单次抽动来自“实体已停止移动，但 20 tick 移动注视滞后仍被误当成当前移动方向”，以及动作状态先于 GeckoLib 收弓混合尾帧结束而解除精确父骨骼补偿。战后静止收势现冻结最后战斗方向，只有检测到当前真实导航或水平位移才切换到行进方向；客户端在远程动作状态结束后继续保留 8 tick 精确父骨骼补偿，覆盖混回待机的尾帧。
- 2026-08-23 的异机日志与后续需求对齐确认，击杀后的手部抽动不能再用“等待上一支箭”或离散取消轨道补丁处理。旧的 4 tick 主箭飞行守卫和 8 条 `reload_cancel` 已移除；连续取箭改为目标无关的 `NOCK`，目标只在搭箭完成后决定能否进入 `DRAW`，从根源避免飞行投射物击杀目标时把右手重置到取箭第一帧。普通主动索敌仍只获取可见目标；已经进入战斗的目标失去视线时拥有最多 40 tick 重定位时间，超时输出 `[EgyptianArcherSight]` 并释放锁定，避免隔墙、隔地持续瞄准。
- 主动警戒会把实现敌对接口的生物纳入目标，而不只局限于普通 `Monster`。普通候选必须位于真正的 24 格球形警戒范围内；幻翼只有同时处于 24 格实际射程且存在视线时才会被持续锁定，飞到不可射击的高空后会放弃目标，等再次俯冲进入射界后重新获取，避免举弓抬头并无期限跟随。玩家攻击末影龙身体部件时会把仇恨来源归一到末影龙本体；射击时在 24 格内选择最近且可见的龙体部件作为瞄准点，箭矢命中部件后通过部件转发伤害到本体，从而支持飞行中的末影龙而不要求瞄准本体中心。
- 模型源文件保存于 `assets-source/blockbench/egyptian_archer_echo.bbmodel`，用户提供的 GLTF 归档于 `assets-source/gltf/egyptian_archer_echo.gltf`。`scripts/update_egyptian_visual_assets.py` 将源动画规范化为 GeckoLib 的 `idle`、`walk`、`hurt`、`draw_bow`、`backstep_jump`、`backstep_shoot`、`melee_attack` 与 `shoot` 运行时资源，并从原动作连续姿态生成首次/连续 `NOCK`、首次/连续 `DRAW`、静态满弓 `AIM`、纯松弦 `SHOOT`、12 tick 反向收箭与缓慢收弓片段。连续取箭轨道会按 GeckoLib 实际 Catmull-Rom 公式采样模型师曲线，把根级 `bone7` 的原始全局姿态转换为相对 `Lower Right Arm` 不变的 48 Hz 密集关键帧；手持箭显隐轨道强制线性插值。脚本同时校验右手相对矩阵误差、阶段边界姿势、缩放范围与松弦中点显隐，并导出模型内嵌实体贴图；当前运行时贴图与新版 Blockbench 源文件保持一致。

#### 2.4.3 中国关刀战士已冻结规格（2026-08-25）

- 实体标识为 `guandao_warrior_echo`，遗物标识为 `guandao_warrior_relic`，中文名为“中国关刀战士英灵”，英文名为 `Chinese Guandao Warrior Echo`。1 级基础属性为 30 生命、12 护甲、7 攻击、0.26 移速、30% 击退抗性；基础攻击间隔 34 tick，满级生命与攻击分别为 60 与 14。
- “甲胄傍身”为可独立关闭的被动：开启时受到的投射物伤害降低 50%，投射物造成的最终击退降低 95%。该远程击退值是最终目标值，不再与基础 30% 击退抗性重复相乘；关闭后从下一次伤害结算起立即失去两项保护。
- “越战越勇”为可独立关闭的被动：开启时每次实际伤害到至少一个敌人的挥击获得一层，每层提高 6% 攻击力，最多五层，持续 8 秒；任何有效挥击刷新全部层数，同一扇区命中多人仍只增加一层，连招四段可分别增加一层。刀刃附近始终使用红橙火焰并随层数从零星逐步增密；五层不再换成蓝色灵魂火焰，而是以更密集的火焰和约每 6 tick 一次的红金火星脉冲表示巅峰。生命越低还会获得越高的最终伤害减免，按缺失生命线性增长并在濒死时达到 30%；火焰、窒息/卡墙及绕过效果或抗性的伤害不享受该减免。关闭时立即清空现有层数，并停止攻击增幅和缺失生命减伤。
- “偃月刀”为可独立关闭的被动：开启时普通攻击在动画约第 9 tick 进行一次服务端判定，对前方 220°、3.25 格扇区内所有有视线的合法敌人造成完整伤害；每次挥击只结算一次，不使用逐帧刀刃网格碰撞。关闭后保留同一普通攻击动画，但只对起手锁定的单个目标造成一次基础近战伤害。
- “关刀连段招式”为可独立关闭的自动主动技能。冷却 12 秒并从起手时开始计算；英灵每次实际受到近战或投射物伤害，剩余冷却缩短 10 tick，连招进行中同样生效且不设累计上限，因此承受足够多伤害时允许在当前连招结束后立刻再次起手。关闭只阻止新连招起手，已经提交的完整连招继续完成，冷却也继续流逝而不会重置。召唤器只以径向遮罩显示这项普通冷却，不在右下角显示充能数字。目标位于 4.5 格内且视线、起步地形安全，并满足附近至少两个有效敌人或目标当前生命高于约两次普通攻击时才起手；一旦起手就完整播放约 5.04 秒动作，除死亡、移除、遣散或遗物失效外不主动中断。死亡或进入濒死状态会在伤害结算当刻取消动作控制器、连招状态与后续命中窗口，倒下后不能再补出后续挥击或“最后一击”。四次挥砍音效固定绑定各自攻击关键帧并在挥空时照常播放：前三段使用逐步加重的横扫声，第四段叠加低沉强力攻击、大型风弹爆发与重锤落地冲击声，并在前方生成一次小型定向风爆；这些反馈不额外造成伤害或击退。起手至第一击前会持续小幅修正朝向，并按实际距离追加最多 0.75 格的碰撞与落差安全追踪位移，优先确保最初锁定的主目标进入第一段扇区；群体覆盖为次要目标，第二至第四段继续使用原有段间智能转向。
- 四个命中窗口位于起手后的第 23、43、58、77 tick，各允许前后 1 tick 容差，但同一阶段对同一目标只伤害一次。四段伤害倍率为 70%／100%／70%／180%，服务器分别使用 150°×3.0 格、190°×3.5 格、120°×3.2 格和 220°×4.0 格的简化扇区；段间可向新目标最多转向约 40°，实际挥击期间锁定朝向。
- 连招四段原始挥击推进分别为 0.6／0.8／0.6／1.0 格，总计 3 格；第一段前另有按需使用、最多 0.75 格的主目标追踪修正。专用步高允许平滑跨越一格高低差；墙体碰撞或前方超过一格的落差只停止当前推进段或追踪修正，连招仍会继续。第一段伤害结算后会覆盖原版受击击退，而不是在其上再次叠加推力；最终给予受击者约 0.04 的水平速度和 0.22 的向上速度并按击退抗性衰减，形成近乎原地的短浮空，使目标不易在第二段前飞出范围。第二段约 10 tick 内检查正面 180°、3.5 格范围，把正在接近且拥有敌对生物主人的投射物向侧前方弹开，保留原速度与原主人，不主动反击投射物主人。
- 普通攻击和连招一旦起手，完整全身动作不会被受伤动画替换；两种攻击均持有最后姿势，动作期间强制把下层移动控制器稳定在待机，并在服务端时限结束时以零 tick 动作层过渡显式停止，避免暴露旧步行动作或在连招末尾的等价 `±180°` 欧拉角之间走长路径。没有已承诺动作时，3.25 格有效近战距离内从任意方向造成实际伤害的合法近战攻击者会被立即锁定，受击动画被取消，并以每 tick 最多约 30° 的速度平滑转身；身体与目标方向误差降至 25° 内以前只转身、不提交普通攻击，起手时再精确锁定本次目标方向，伤害扇区也固定使用这份方向。攻击或连招进行中出现的新伤害来源会排为下一位反击目标，不改变已经合法起手的挥击；若原目标在 9 tick 出刀前摇中死亡，当前动画可以自然挥空，但不会中途转向新敌人。远程或距离外攻击者会被记录为追击目标，但允许当前 10 tick 受击动作播完，连续受击不会重新起播。实际受伤、疼痛眨眼、表情和伤害来源注视仍正常结算。
- 关刀动画诊断当前为复查后方反击朝向而临时默认开启。服务端用 `[GuandaoAnimationServer]` 记录攻击、命中窗口、追踪阻挡、反击排队/提升、朝向等待和动作结束边界，客户端用 `[GuandaoAnimationBonesClient]` 采样攻击/连招结束前后约 5 tick 的关键骨骼及最大帧间突变量；本轮回归确认后应恢复默认关闭，也可随时使用 `/echo_warrior animation_debug guandao on|off` 对玩家 32 格内最近的所属中国关刀战士切换诊断。
- 中国关刀战士复用其他战士的完整视觉生命感规则：自然注意、玩家主动对视、被主人发现偷看后的反应与离场、自然/双重眨眼、歪头、独立瞳孔追踪、苦力怕警觉、快速接近目标与伤害来源注视。运行时按 `Head`、`Eyes_Left`、`Eyes_Right`、`Eyebrow` 映射面部骨骼，并对 `Main` → `Body` → `Upper_Body2` 父链进行模型专用补偿；战斗目标、普通攻击和连招始终保留身体朝向优先级。
- 模型规范源保存于 `assets-source/blockbench/guandao_warrior_echo.bbmodel`，用户提供的 GLTF 仅归档于 `assets-source/gltf/guandao_warrior_echo.gltf`；后续仅提供 BBModel 即可由代码导出 GeckoLib `geo`、动画与内嵌贴图，无需用户额外导出 GLTF。`scripts/update_guandao_visual_assets.py` 将原 `idle`、`animation2`、`attack`、`技能`、`hurt` 规范化为 `idle`、`walk`、`attack`、`combo`、`hurt`，并在武器骨骼下追加 `WeaponParticleAnchor` 供刀刃粒子跟随真实动画位置。阿兹特克勇士、埃及弓箭手与日本武士的新版遗物图标均已同步到运行时、召唤器与百科。

#### 2.4.4 日本武士已冻结规格（2026-08-26）

- 实体标识为 `japanese_samurai_echo`，遗物标识为 `japanese_samurai_relic`。定位锁定为中甲、高机动、依赖闪避循环的单体决斗者；1 级为 28 生命、6 护甲、6 攻击、0.32 移速、20% 击退抗性，满级生命与攻击为 56 与 12。普通攻击基础周期为 40 tick，并随最终攻速在 24～80 tick 内缩放；该周期是当前平衡基准，不代表未来永远禁止数值调整。`刺` 与两种冲刺动作不受普通攻速缩放。
- “残心”为可独立关闭的闪避被动。开启时基础概率按缺失生命平滑线性增长：满血 20%，1 点生命 60%；每次实际完成一次伤害段后额外获得 20 tick（1 秒）、20 个百分点的闪避概率，重复命中只刷新、不叠加，最终上限 80%。近战、投射物、爆炸及范围攻击均可被整次闪避；已经存在的燃烧、中毒、流血等持续状态及环境伤害不能触发闪避，也不会因后续闪避被清除。每次成功闪避立即获得一层“踏込”充能。关闭时立即清除攻击后的临时闪避加成并完全停止闪避判定。
- 闲置、行走或普通追敌时成功闪避会播放 `dash_backward`，沿远离伤害来源的安全方向约后撤 2 格、持续约 14 tick；墙体、危险落脚面或超过一格高差会缩短或阻止位移。普通攻击、`踏込`、`刺`、受伤等完整动作中触发闪避时，不中断当前动作、不移动本体，仅在反向或侧向生成残影演出。`HURT` 同样不得打断普通攻击或技能，只能在未承诺完整动作时播放。
- “踏込”为可独立关闭的自动位移，最多 3 层，遗物初始拥有 3 层，每 100 tick 按世界时间恢复一层；充能、恢复进度与离场时间保存在遗物上。仅当当前目标位于普通攻击范围外但不超过 8 格、存在视线且整条地面路径预检安全时自动起手；目标已经进入近战范围时保留充能。冲刺速度约 0.45 格/tick、持续 6～14 tick、在距离目标约 2 格处停下，动作期间免疫不绕过无敌的伤害。持续时间按实际移动步数计算，结束 tick 会先完成最后一次位移并立即复查到达条件，不得因少执行一步而在目标前提前结束。该动作是贴地冲刺而非飞行：保留重力与垂直速度，可平滑通过一格高低差，但不会主动跨越超过一格的落差、墙体、二格障碍、低顶或危险落脚面；途中环境变化时停在最后安全位置，不能悬空等待。只有成功抵达并在同一 tick 提交后续普通攻击时才消耗一层，受阻、失去目标或没有接上攻击时不扣层；无论成功或失败均进入 10 tick 内置冷却。关闭只阻止新的踏込，已起手的冲刺允许完成；充能与恢复进度保持不变。
- “斩”为可独立关闭的普通攻击扩展，并按动画语义拆为分支动作：`attack_1` 为第一刀后收回待机，`attack_2` 为同一第一刀后继续第二刀，`attack_3` 暂不使用。开启时第一刀在前方 90°、2.25 格判定，锁定主目标承受 100%，同扇区其他目标承受 50%；第一刀使用不触发原版受击击退的专用伤害类型，再单独施加 0.08 的极轻水平击退，避免目标在第二刀前被推出范围。第一刀从起手到命中帧会以最多 0.30 格/tick、累计最多 1.5 格追随起手时锁定的目标，并在约 1.7 格处停止靠近。第一刀结算后立即检查前方 160°、2.75 格内仍有视线的合法目标，若存在则在同一预载动画序列中无待机帧接续第二刀，否则立刻切入收势。第二刀可覆盖此刻刚进入扇区的新敌人，但背后靠近或隔墙出现者不计入；分支确认后才进入的目标也不再追加。第二刀仅向分支确认时记录的主目标追随，最多 0.20 格/tick、累计最多 0.75 格；伤害仍只作用于分支时保存的集合，主目标 100%，其他目标 50%，并保留终结击退。两段追随每 tick 最多修正 15°，相对该段起始朝向累计不得超过 ±45°；目标死亡、无效、失去视线、转出角度或路径不安全都会终止本段追随，但不会中断攻击。追随可安全平滑通过一格高低差，不穿墙、不跨危险落差，不消耗踏込、不提供无敌或残影，只产生轻微脚下扬尘。关闭后保留第一刀动画、追随与对锁定目标的单次 100% 基础伤害，但移除第一刀范围伤害和第二刀分支。
- “刺”为可独立关闭的自动主动技能，冷却 200 tick 并从起手时开始，冷却结束时间保存在遗物上。关闭只阻止新动作起手，已经提交的刺仍完整完成，冷却继续流逝。动作始终以模型原始 1.0 倍速度播放，不受普通攻击速度的提高或降低影响；目标位于 2.25 格内且有视线时优先于普通攻击起手，起手到第一段前最多每 tick 转向 20°，刺入后锁定方向。沿前方 2.75 格、半径 0.4 格的胶囊范围命中任意数量紧密排列且有视线的敌人：刺入造成 150% 攻击伤害、无击退，并把实际命中者短暂停在原地约 3～4 tick；拔刀只对第一段命中集合造成 300% 攻击伤害、轻微普通击退与红色水尘粒子。两段共同使用只绕过原版受伤冷却、但不绕过护甲与无敌状态的专用伤害类型，确保相隔 4 tick 时仍分别完整结算 150% 与 300%，且不会清除燃烧、中毒或其他持续状态。目标在第一段后死亡时不再承受第二段伤害，但仍在保存的命中位置播放血液演出。当前命中时间固定为起手后 16 与 20 tick，等待模型师后续动画时再校准。
- 战斗动作优先级为：目标在普通范围外且“踏込”可用时先冲刺；目标已在近战范围且“刺”冷却完成时先刺；否则使用普通两刀分支。所有伤害、闪避、充能、冷却、命中集合与短暂停身均由服务端裁定；残影只在客户端渲染，不参与碰撞与判定。
- 日本武士现已接入与其他英灵一致的完整视觉生命感规则，包括自然注意、玩家主动对视、被主人发现偷看后的反应与离场、自然／双重眨眼、歪头、独立瞳孔追踪、苦力怕警觉、快速接近目标和伤害来源注视。服务端与中国关刀战士复用同一套视觉状态机；客户端按日本模型真实的 `Main` → `Body` → `Upper_Body` → `Upper_Body2` → `Head` 父链补偿。普通攻击、刺、踏込、后撤与受伤动作播放时保留模型师对 `Head` 和 `Eyebrow` 的控制，程序化头部与眼部姿势在后台回归中立，动作结束后平滑恢复，避免两套动画争抢骨骼。社交对视与害羞离场在存在战斗目标或任何已承诺动作时立即让位；表情只在本体正常渲染时更新，残影冻结事件当帧已经合成好的最终姿势，不会在回放时继续转眼或眨眼。
- 残影使用冻结的 GeckoLib 骨骼姿态快照，不创建实体、AI、碰撞、阴影或可交互对象，并正常进行深度测试而不能透墙。真实后撤在约 0%、35%、70% 路程留下约 10 tick 快照，忙碌动作闪避生成三道错位 0.35／0.70／1.05 格、寿命 8／7／6 tick 的幻影，踏込从起点开始每约 1.25 格留下快照、最多 5 道。踏込与真实后撤只在本体已经经过的上一服务端位置采样，禁止残影因客户端位置插值而跑到本体前方。默认使用原始贴图并开启 2A：快照先完整保持约 2 tick，再按附着于模型 UV 的固定噪声溶解；遮罩由约 70% 有机噪声与 30% 弱方向梯度组成。2A 使用专用半透明溶解管线，把溶解阈值与真实 alpha 分开传递；每道快照从自身初始不透明度开始按年龄平滑递减，因此时间线上最早生成的残影会最先变淡并消失。额外附色改为指令开启的可选模式，使用覆盖完整明暗范围的高对比灰度派生贴图乘以残心青或踏込金，保留甲片、布料与刀身结构。2A 主体使用场景光照，不强制全亮；原先用于制造窄亮边的第二次白色整模型绘制会在 GeckoLib 提交顺序下覆盖主体，因此与 2B 一样暂时移除。关闭 2A 时回退到第一阶段半透明快照。
- 单个武士残影上限 8 道，客户端高质量软上限 64、总硬上限 96；48～63 道会降低新残影密度，64 道起只保留已有粒子回退。2A 管线发生运行时异常时自动回退第一阶段，并每 100 tick 尝试恢复。三个临时调试指令均作用于 32 格内最近的所属武士：`/echo_warrior visual samurai_afterimage_neutral on|off`、`/echo_warrior visual samurai_afterimage_advanced on|off`、`/echo_warrior visual samurai_afterimage_outline on|off`。新召唤的武士默认开启 2A 且使用原始贴图；输入 `samurai_afterimage_neutral off` 才开启残心青／踏込金附色，输入 `neutral on` 恢复无额外附色的原始贴图。2B 因 GeckoLib 自定义几何直接走原版 outline 通道会错误绘制成实心白色剪影而暂时停用，`samurai_afterimage_outline on` 会拒绝开启并说明原因；待以后实现独立后处理描边再恢复。设置只保存在当前召唤实体的同步数据中，重新召唤或重启后重置，不写入遗物。
- 新版模型源保存于 `assets-source/blockbench/japanese_samurai_echo.bbmodel`，由 `scripts/split_samurai_animations.py` 拆分普通攻击分支，并由 `scripts/update_samurai_visual_assets.py` 导出 39 骨骼、12 段 GeckoLib 动画与模型内嵌贴图。四个 16×16 技能图标按用户九宫格映射精确裁切：左上残心、右上斩、中央刺、左下踏込；原始技能表归档于 `assets-source/textures/gui/summoner/skills/japanese_samurai_skills.png`。

每个英灵需要具备：

- 独立外观和轮廓。
- 明确的战斗定位。
- 召唤、攻击和消散反馈。
- 稳定的跟随、传送和目标选择行为。
- 可靠的水域生存与脱困行为：当前所有英灵都会使用原版浮水目标并免疫溺水；完全没入水中且约 2 秒没有明显上浮或接近主人时，跟随模式会暂停战斗并尝试游向主人，约 5 秒仍卡住时在主人附近寻找干燥地面或水面进行安全传送。等待与闲逛模式不会被强制传回主人，追猎、举盾冲锋等专用位移期间也不会被水下传送打断。

## 3. 比赛版范围

### 3.1 第一阶段：可游玩的军团兵原型

第一阶段只验证“召唤器—英灵”核心闭环，不等待正式考古系统、GUI、能量或完整动画：

1. 独立的 Echo Warrior 创造模式标签页，召唤器作为图标。
2. 不可堆叠的 `test_echo_summoner`，每个物品实例持有唯一标识。
3. 每个召唤器最多绑定一个 `roman_legionary_echo`；多个召唤器可以各召唤一个英灵。
4. 普通右键打开召唤器 GUI。
5. 潜行右键快速召唤或召回：没有已召唤英灵时在玩家前方召唤，已有已召唤英灵时将其召回玩家附近。
6. 英灵跟随召唤者，只在召唤者攻击、召唤者受击或自身受击后反击，不主动扫描并攻击普通生物。主人距离超过 5 格时开始跟随，进入 5 格内停止；超过 32 格才使用安全传送恢复。
7. 英灵召唤后持续存在，不再使用 120 秒自然消散。召唤器离开原持有者物品栏后有 5 秒粘滞时间；宽限超时、进入容器或转交他人均解除旧绑定并使英灵离场。
8. 玩家登出或跨维度时遣散，回到游戏后由玩家手动再次召唤。
9. 使用军团兵模型师交付的待机、行走、攻击和受伤动画；召唤与消散暂时使用原版粒子及声音反馈。举盾和收盾动画已导入，但在盾牌技能确定前只允许通过开发指令预览。

第一阶段最初明确不包含考古、回声罗盘、遗物词条、召唤能量、升级、GUI、自动召唤、致命伤保护、伤害分担、跨维度跟随及额外英灵；此清单保留为历史范围记录。项目随后已经通过显式设计决策推进考古、回声罗盘、22 项遗物天赋、召唤能量、升级、GUI 与第四、第五位英灵。

MVP 必须先完成：

1. 一个回声罗盘。
2. 一套可重复测试的考古获取流程。
3. 一个召唤器和一种基础灵魂能量流程。
4. 三个可正常跟随和战斗的英灵。
5. 三项简单升级。
6. 完整的声音、粒子、模型、材质和基础英文文本。

以下内容继续保持为 MVP 稳定后的候选，不因天赋系统完成而自动纳入：

- 自动召唤。
- 致命伤害保护。
- 伤害分摊。
- 饰品提供的扩展背包或额外分页 GUI。

## 4. 技术基线（已冻结）

- 目标游戏版本：Minecraft Java Edition 26.1.2。
- 加载器：Fabric Loader 0.19.3。
- Fabric API：0.155.2+26.1.2。
- Java：25，项目私有运行时位于 `.toolchains/jdk-25`，不修改系统 `JAVA_HOME`。
- SmartBrainLib：2.0.0，必需前置。
- GeckoLib：5.5.2，必需前置。
- 构建：仓库内 Gradle Wrapper，不依赖系统全局 Gradle。
- IDE：IntelliJ IDEA 2025.3 或更高版本。
- 版本控制：Git。
- 模组 ID：`echo_warrior`。
- Java 根包：`com.yuriscat.echowarrior`。

选择 Fabric 的主要理由是开发运行、调试热替换和 26.1 文档路径比较清晰。上述版本均已在 2026-08-07 依据官方项目的 26.1.2 分支或发布信息核对并冻结；升级必须作为单独任务处理。

### 4.1 实体 AI 与依赖策略

英灵需要共享但可组合的感知、记忆和行为，例如：

- 记住召唤者并维持合理跟随距离。
- 发现正在攻击召唤者的敌人。
- 在跟随、保护、攻击、返回和消散之间切换状态。
- 根据英灵类型组合近战、冲锋、格挡或远程行为。
- 在寻路失败、距离过远或跨越地形时安全返回召唤者。

确定将 SmartBrainLib 作为必需前置，并在其上编写 Echo Warrior 专用的 Sensors、Memories 和 Behaviours。第一阶段允许把严格的物品绑定与生命周期检查放在实体服务逻辑中，但移动、目标和战斗活动应走 SBL brain 行为。

截至 2026-08-07，SmartBrainLib 已提供 Minecraft 26.1.2 的 2.0.0 版本，覆盖 Fabric、Forge 和 NeoForge。集成时仍必须用最小英灵原型验证：

1. 26.1.2 构建与开发客户端可以稳定启动。
2. 文档和 2.0.0 API 足够完成跟随、护主、目标选择及战斗行为。
3. 服务端和单人游戏行为一致。
4. 不会明显妨碍热替换和测试效率。

不在比赛周期内完整重写 SmartBrainLib。若验证失败，则退回原版 Goal 系统，并只实现 Echo Warrior 所需的小型共享 AI 组件。

确定将 GeckoLib 作为英灵模型和动画系统的必需前置。目标为 Minecraft 26.1.2 时，对应可用版本为 GeckoLib 5.5.2，并应选择与最终加载器严格匹配的构建。

不使用 Kotlin for Forge：Echo Warrior 使用 Java 开发，SmartBrainLib 与 GeckoLib 均不要求 Kotlin for Forge。只有未来明确引入 Kotlin 源代码时才重新评估。

当前机器检查结果：

- Git 已安装。
- 默认 Java 是 17，不能作为 26.1 的最终开发 JDK。
- 未安装全局 Gradle；这是正常状态，后续使用 `gradlew.bat`。

## 5. 开发与人工测试约定

### 5.1 一键测试目标

项目最终提供：

```text
scripts/run-test-client.ps1
scripts/run-test-client.bat
scripts/playtest-now.ps1
scripts/playtest-now.bat
```

`run-test-client.bat` 是面向 CMD 和双击启动的入口，并转交给 PowerShell 主脚本；避免 Windows 将 `.ps1` 当作普通文件打开。

`playtest-now.bat` 是日常人工测试的最短入口：编译并启动开发客户端，然后通过 Quick Play 直接进入现有的 `CATTEST` 世界。对应 PowerShell 脚本要求该世界已经存在，避免名称错误时静默停在主菜单。

当用户说“测试”“启动 MC”“进测试世界”等表达时，Codex 应：

1. 检查是否已有开发客户端在运行，避免重复启动。
2. 编译当前代码并报告阻塞性错误。
3. 使用 Gradle `runClient` 启动开发客户端。
4. 通过 `--quickPlaySingleplayer` 自动进入固定测试世界。
5. 保留控制台和游戏日志，崩溃后主动定位原因。

一键测试脚本会在本项目 Minecraft 窗口创建完成后尝试恢复窗口并置于前台，避免游戏已经锁定鼠标但窗口仍被其他程序遮挡；该观察器仅在启动阶段短时运行，不常驻后台。

当前固定测试世界目录名：

```text
CATTEST
```

首次需要人工创建世界时，只做一次。之后启动脚本应直接进入该世界。

### 5.2 测试世界原则

- 测试世界只用于开发，不作为正式展示存档。
- 默认开启作弊，方便传送、切换模式和获取物品。
- 建立固定测试区域：考古区、召唤区、战斗区和性能测试区。
- 需要可重复场景时，优先添加开发命令或测试结构，而不是每次人工摆放。
- 对破坏性测试保留单独世界或备份，避免污染主测试世界。

### 5.3 热加载分级

能不重启时，不重启整个客户端：

- 材质、语言、模型和部分客户端资源：优先使用资源重载。
- 配方、战利品表、标签等数据：优先使用 `/reload` 或对应重载流程。
- Java 方法内部逻辑：调试模式下尝试 HotSwap。
- 新增或删除字段、方法、类，以及修改注册表、实体类型、Mixin 结构：通常需要重启客户端。
- 世界生成相关改动：通常需要新建区块；必要时重建测试世界。

如果使用 IntelliJ 调试运行，后续评估 JetBrains Runtime 的增强类重定义参数：

```text
-XX:+AllowEnhancedClassRedefinition
```

它可以提高 Java 热替换覆盖范围，但不能承诺所有模组改动都能安全热加载。

### 5.4 Codex 的测试职责

- 修改代码后先运行与改动规模匹配的检查。
- 能通过自动化测试或 GameTest 验证的行为，不把重复操作推给用户。
- 需要视觉、手感或声音判断时，才请用户进入游戏体验。
- 启动前说明本轮最少需要用户验证的内容。
- 测试后结合 `logs/latest.log` 和崩溃报告继续修复。
- 不以“成功启动”为完成标准；核心场景必须实际可用。

### 5.5 召唤器 GUI 与容器交互

- 当前召唤器界面已经接入真实服务端容器：六个饰品槽、一个燃料槽、一个遗物槽，以及玩家主物品栏和快捷栏均使用原版槽位交互，不再通过客户端手工绘制物品来伪装容器。
- 打开界面的召唤器通过物品实例 UUID 验证并锁定；召唤器内部八个槽位的物品保存在该召唤器物品的数据组件中。物品栈因同步而更换 Java 对象引用时，仍按 UUID 找回当前物品并保存内容。
- GUI 不再显示“布局”按钮，也不再提供拖动区域、方向键微调、保存、撤销或重置等界面内布局编辑能力；当前布局作为正式界面使用。
- 鼠标悬浮空饰品槽时依次显示“饰品槽”“在此安装英灵饰品”“同名饰品不能重复安装”。槽内已有物品时不显示这组空槽说明，改由原版槽位系统显示物品自身的悬浮框；取出物品后，空槽说明恢复。

### 5.6 Blender 动画导出工具

- 英灵美术工作流固定为：Blockbench 负责模型、UV 和基础骨架，经 glTF 导入 Blender；Blender 负责 Actions；模组运行时直接使用 GeckoLib `.animation.json`，不把 `.bbmodel` 当作运行时依赖。
- 所有英灵的战斗动作状态机、Once／Loop／Hold 选择、攻速与事件帧缩放、控制器骨骼所有权、目标死亡收尾及防抽搐验收统一遵循 `docs/ANIMATION_STATE_MACHINE.md`；角色特有的动画名、骨骼名和数值仍保留在各自代码或资产脚本中。
- 第一版 Blender 扩展源码、测试、说明和安装包放在 `human-work/Blender 研究/`，与 Fabric/Gradle 正式源码隔离。单个扩展包支持 Blender 4.5～5.2，并把目标骨架的全部兼容 Actions 合并导出到一个 GeckoLib 动画文件。
- 导出时逐帧计算 Action 的最终骨骼姿态，使约束、已有 IK、驱动器和 F-Curve 修改器进入结果；随后使用可调误差简化关键帧，不修改源 Action。NLA 混合、逐顶点网格形变、动画事件、IK/FK 自动搭建和一键启动 Blockbench 不属于第一版。
- 插件登记从 BB/glTF 导入的原始骨架清单；额外辅助骨忽略，原始骨骼删除、改名、改变父子关系或 Rest Pose 时阻止导出。根骨骼位移完整保留，但水平根位移需要警告，实体真实移动仍由服务端模组逻辑负责。
- 每个 Action 的 `Once / Loop / Hold` 设置、输出路径和动画 ID 前缀随 `.blend` 保存。覆盖运行时 JSON 前创建 `.bak`，再通过同目录临时文件原子替换。
- 武士模型使用 `human-work/Blender 研究/scripts/build_samurai_control_rig.py` 生成模型专用控制骨架：保留 42 根原始游戏骨，另外添加可视化手脚、头部、躯干、武器/刀鞘控制器，四肢 IK 以及肘膝 Pole Target。辅助骨不会进入运行时 JSON；这不代表已经实现任意模型的通用自动绑定。

### 5.7 交互式回声档案馆

- 网页优先的交互式百科位于 `encyclopedia/`，当前名称为 `Echo Archive / 回声档案馆`。
- 界面采用持续存在的双栏结构：左侧为可拖拽、缩放并按大分类切换的节点知识地图，右侧为当前条目的连续滚动内容；切换条目不会进入翻页式书本界面。
- 当前分类为总览、探索与考古、遗物与天赋、召唤系统、英灵、成长与升级、传承、英灵饰品。节点使用人工固定坐标和显式连线，不采用会自动漂移的力导向布局。
- 分类、节点坐标、连线、状态与中文条目当前统一保存在 `encyclopedia/content/atlas.zh_cn.json`。网页只负责渲染和交互；未来游戏内版本应转换或直接读取同一套中立数据，而不是复制第二份百科内容。
- 当前网页复用项目中人工制作的物品、技能、模式和属性图标；饰品配方格另外复用从本地 Minecraft 26.1.2 客户端资源提取的原版物品图标，并通过悬浮名称模拟 JEI 查表。它不是 CurseForge 宣传美术，也不改变禁止生成 AI 项目头像或画廊素材的要求。
- 游戏内百科属于后续阶段；网页实现期间只为原生 Minecraft 界面保留数据与交互可迁移性，不在比赛 MVP 中增加新的运行时依赖。
- 玩家可见的玩法、数值、操作、反馈或实现状态发生变化时，应在同一次任务中同步更新相关百科条目，或明确说明该改动不影响百科。

### 5.8 美术制作任务板

- 面向模型师、美工与验收人员的独立单文件任务板位于 `docs/ART_PRODUCTION_BOARD.html`，直接使用浏览器打开，不需要安装依赖或启动本地服务。
- 任务板支持四阶段看板、拖动改状态、负责人/类型/优先级筛选、交付路径、验收清单和进度统计；数据保存在当前浏览器的本地存储中。
- 跨电脑或成员交接通过任务板内置的 JSON 导出与导入完成。任务板不替代 `PROJECT.md` 的设计来源地位，也不把其中的示例或工作流任务自动提升为比赛 MVP 范围。
- 此工具只改变团队美术协作流程，不改变玩家可见内容，因此新增任务板本身不需要同步百科条目。

### 5.9 测试员离线交付与 Git 更新

- 零 Git 经验测试员使用项目根目录的 `TESTER_GUIDE.html`。指南提供“第一次使用”和“每次强制更新”两个标签页，并通过浏览器本地存储保留上次选择。
- Windows 10/11 x64 测试员的完整离线包生成到被 Git 忽略的 `temporary-delivery/`。交付目录包含带 `.git` 的当前项目、PortableGit、项目 Java 25、`CATTEST` 世界、项目本地 Gradle User Home 和已验证的离线构建缓存；测试员解压后不需要安装系统 Git、Java 或设置系统环境变量。
- `首次安装.bat` 只检查离线包完整性、私有仓库远程地址、`main` 分支、Java 25 和离线编译，不进行首次联网下载。`启动测试.bat` 为测试员进程临时设置项目内 `GRADLE_USER_HOME`，再调用现有 `scripts/playtest-now.ps1`。
- `强制更新.bat` 要求先关闭开发客户端，再执行 `git fetch --prune origin main`、`git reset --hard origin/main` 和保留测试员入口的 `git clean -fd`。它抛弃源码修改和非忽略散落文件，但保留 `.toolchains/`、`run/`、测试世界、配置、截图、日志与离线缓存；禁止使用会清除忽略文件的 `git clean -fdx`。
- 私有仓库访问由项目所有者将测试员 GitHub 账号添加为协作者；PortableGit 内置的 Git Credential Manager 负责首次在线更新登录。更新脚本不自动启动 Minecraft。
- 每次更新后执行离线 `classes` 检查。若版本升级引入离线包中不存在的新 Gradle、Minecraft、Fabric、SmartBrainLib 或 GeckoLib 文件，脚本停止并要求开发者重新生成支持包，不自动进行长时间联网下载。
- 离线交付工具只改变测试协作流程，不改变玩家可见玩法，因此本节变更不需要同步百科条目。

### 5.10 模型师便携更新与启动

- 已经配置好项目、Java 25 和开发依赖的模型师使用独立轻量工具包，不复用测试员离线包。工具包不包含项目源码、JDK、Gradle 离线缓存、测试世界或 Minecraft 依赖，只携带 PortableGit、Git Credential Manager、中文 HTML 指南和更新/启动脚本。
- 模型师工具包必须放在 Git 项目目录之外。第一次运行通过目录选择窗口指定已有 Echo Warrior 根目录，路径保存在工具包自己的 `state/project-root.txt` 中；随后所有双击入口复用该路径，并提供单独的“重新选择项目目录”入口。
- `安全更新项目.bat` 是日常默认入口：只为当前 BAT/PowerShell/Git 进程临时设置 `HTTP_PROXY` 与 `HTTPS_PROXY` 为 `http://127.0.0.1:7897`，不写入系统环境变量或 Git 全局配置；随后要求客户端已关闭、当前处于 `main`，并在任何已跟踪修改、未跟踪文件或未推送本地提交存在时停止。只有工作树干净且本地没有领先提交时，才执行 `fetch` 与 `--ff-only` 快进更新。
- `强制覆盖更新（自动备份）.bat` 使用相同的进程级 GitHub 代理，是显式恢复入口：先成功获取远程 `main`，显示本地变更并要求输入 `COVER`，再用包含未跟踪文件的 Git stash 保存未提交工作，并为可能被覆盖的本地 `main` 提交建立 `modeler-backup/<时间>` 分支，最后切换到远程 `main` 并执行不清除忽略文件的 `git clean -fd`。代理不可用、远程获取失败或任何自动备份失败都会在覆盖前停止。
- `启动开发客户端.bat` 不提供离线环境；它按项目内 `.toolchains/jdk-25`、系统 `JAVA_HOME`、系统 PATH 的顺序寻找 Java 25，使用仓库 Gradle Wrapper 编译并启动。存在 `CATTEST` 时直接进入，否则进入主菜单；已运行的开发客户端不会重复启动。
- 项目根目录提供 `启动本地百科.bat`，开发者与模型师在各自的本地 Git 项目中均可直接双击。它定位 `encyclopedia/`，要求 Node.js 22.13 或更高版本，在首次启动或锁文件更新时自动执行 `npm ci`，固定使用 `127.0.0.1:4173` 启动网页并自动打开默认浏览器；检测到同一地址已经是回声档案馆时直接复用。百科运行期间保留启动窗口，关闭窗口即停止服务。
- 模型师便携工具只改变团队美术协作与启动流程，不改变玩家可见玩法，因此本节变更不需要同步百科条目。

## 6. 预计项目结构

```text
mod-project/
├─ PROJECT.md
├─ AGENTS.md
├─ README.md
├─ build.gradle
├─ settings.gradle
├─ gradle.properties
├─ gradlew
├─ gradlew.bat
├─ gradle/
├─ scripts/
│  ├─ run-test-client.ps1
│  └─ playtest-now.ps1
├─ src/
│  ├─ main/
│  └─ gametest/
├─ run/
│  ├─ saves/CATTEST/
│  └─ logs/latest.log
└─ docs/
```

`run/`、构建产物和本地测试世界不提交 Git；必要的测试结构、GameTest 和配置则提交。

## 7. 尚未确定

- `Echo Warrior` 是否作为最终发布名称；改展示名不要求更改 `mod_id` 或 Java 包名。
- 三个英灵的最终美术风格和历史时代。

## 8. 第一阶段固定参数

- 测试召唤器：`test_echo_summoner` / Test Echo Summoner / 测试英灵召唤器。
- 军团兵英灵：`roman_legionary_echo` / Roman Legionary Echo / 罗马军团兵英灵。
- 召唤器物品图标：人工提供的 `16×16` 灰色单帧；“英灵回声”创造模式标签页复用同一图标，不再播放序列帧。
- 英灵属性：30 生命、8 护甲、6 攻击、0.28 移速、32 跟随距离、0.3 击退抗性；基础攻击冷却为 20 tick，并在本次收势或追击动作完整结束后开始计算。
- 跟随参数：距离超过 5 格开始步行跟随，接近至 3 格停止，超过 32 格时只向可站立、无碰撞的安全落点传送；SBL 专用行为每 5 tick 刷新一次原版寻路路径。主人明显悬空且附近没有同高度安全落点时，地面英灵不继续向空气寻路；已经位于主人下方附近时稳定等待，主人水平移动过远后才按节流频率尝试移动到其投影附近的地表安全落点。
- 水域安全：罗马军团兵与阿兹特克勇士均会主动浮水且不会受到溺水伤害；跟随模式下完全没入水中并持续卡住时，先中止战斗寻路并游向主人，最终使用不受常规跟随距离限制的安全传送脱困。
- 朝向反馈：首次召唤及手动召回时，英灵的身体和头部均朝向召唤者。
- 视觉生命感：罗马军团兵、阿兹特克勇士、埃及弓箭手、中国关刀战士与日本武士均由代码接管影子、分层注视目标、玩家对视、头部转向、瞳孔追踪与收缩、眉毛下压眨眼及安全状态下的偶发歪头；瞳孔、头部和身体分别响应目标。普通行走、跟随、闲逛、归位与游泳时，基础注视点改为前进方向，头部侧转限制约 15°、瞳孔额外侧视约 10°，停止后约 0.3 秒内恢复普通观察；战斗目标、苦力怕、伤害来源、快速接近目标和专门的视觉互动仍可覆盖该限制。战斗 AI 始终保留身体朝向控制权，详细约定见 `docs/VISUAL_BEHAVIOR.md`。
- 军团兵动作：新版待机为 3 秒循环、行走为 1 秒循环、受伤为 0.5 秒。普通攻击改为服务端权威的条件二连击：`attack_2` 的前 13 帧形成 0.54167 秒首击段，基础速度下第 6 tick 在 2.25 格判定范围内对原锁定目标造成 100% 伤害；第 11 tick 时，只有同一目标仍存活、合法、处于 2.75 格追击确认范围且保持视线，才无缝继续 0.5 秒追击段，并在整套动作第 16 tick 以 3.10 格判定范围造成 75% 伤害，否则切换到由 `attack_1` 尾段制作的 0.25 秒收势。首击仍受护甲与格挡影响，但不施加原版击退；第二击可正常击退。追击始终只认原目标，不会转打旁边生物；第二次判定前目标死亡、离开范围或被遮挡时只会挥空。动画、两个命中时机和动作后冷却按本次起手时的攻击间隔等比缩放，第二击只绕过普通受伤冷却，仍遵守护甲、友军保护和实际无敌规则。攻击中的受伤不会打断身体动作，但仍会触发代码控制的疼痛闭眼与伤害来源注视；举盾技能、取下遗物与手动召回会取消尚未结算的后续攻击并进入冷却，避免幽灵伤害或通过切换物品跳过动作间隔。
- 军团兵攻击追步与动画分层：攻击仍只在原版近战起手范围内提交，不会从远处直接吸附。首击阶段按基础每 tick 0.30 格、累计最多 1.5 格安全追随原目标，第二段在命中前按每 tick 0.20 格、累计最多 0.75 格继续追随；每段在约 1.7 格处停止靠近，速度随本次动作攻速等比缩放，但累计距离不变。追步每 tick 最多转向 15°、相对本段起始方向最多 ±45°，墙体、危险方块、液体、超过一格的落差或活动边界会立即阻止继续前移，但不会穿越障碍或强行改换目标。运行时三个派生攻击片段只控制上半身、盾与武器，`root`、`body_root`、下半身、双腿及腰布由移动层接管；只有服务器实际完成追步时才同步播放按攻速缩放的行走腿部，停止追步或进入收势时立即回到站立下半身。模型师原始 `attack_1`、`attack_2` 与现有代码表情骨骼均保持不变。
- 玩家主动对视：看向任一已接入英灵的头部需要持续获取时间，12 格内为 0.5 秒，远距离按线性公式增加；战斗中抑制获取，对视时眼睛、头部和身体分阶段自然转向。完整参数以 `docs/VISUAL_BEHAVIOR.md` 为准。
- 被主人发现偷看后的收尾以短距离走开为主要分支；五名已接入英灵都会先转移瞳孔和头部、再转身行走，并在结束后短暂回避重新注视主人，避免刚移开视线就立刻回头。概率、角度与中断规则以 `docs/VISUAL_BEHAVIOR.md` 为准。
- 碰撞箱：宽 0.75、高 1.95，确保可通过常规一格宽、两格高通道。
- 友军保护：不会伤害主人、同主人英灵、主人驯服生物、同队实体、创造或旁观玩家；主人不能伤害自己的英灵。
